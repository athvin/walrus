//! The `walrus-pg-sink` binary — a thin shell over the `pg_sink` library.
//!
//! Real bootstrap / health / replication land in PR 2.18+. For now this only proves the bin links
//! against the lib.

fn main() -> std::process::ExitCode {
    unimplemented!("walrus-pg-sink bootstrap — PR 2.18")
}
