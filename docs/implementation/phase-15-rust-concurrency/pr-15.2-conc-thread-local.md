<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 15.2 — Record why the thread-local scratch rule is superseded

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** superseded by PR 11.6
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 15 — Rust concurrency · **Crates touched:** none ·
> **Est. size:** S · **Depends on:** PR 15.1 · **Unlocks:** PR 15.3

PR 11.6 already gives `BatchBuilder` an explicitly owned temporal scratch buffer, and PR 11.17 records the zero-thread-local invariant. A later `thread_local!` rewrite would duplicate that allocation fix and contradict the established ownership model.

## Why — learning objectives

- Prefer explicit per-builder ownership over ambient thread state when the earlier task already removed the allocation.
- Resolve corpus conflicts once instead of teaching two incompatible final shapes.

## Read first

- [`conc-thread-local`](../../../.claude/skills/rust-skills/rules/conc-thread-local.md) — the source rule.
- `crates/pg-to-arrow/src/batch.rs` — PR 11.6's `BatchBuilder::ts_buf`.
- `docs/implementation/notes/rust-skills/mem-arena-allocator.md` — PR 11.17's zero-thread-local decision.

## Baseline contract

- **Precondition:** Confirm PR 11.6 owns the complete code postcondition on the immediate predecessor commit.
- **Locked outcome:** Make no production or test-code change. Record the ownership proof in the named evidence note.
- **Allowed files:** The evidence note is the only artifact. Any residual uncovered site blocks the task for re-audit.

## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Create `docs/implementation/notes/rust-skills/conc-thread-local.md` proving PR 11.6 owns the allocation change and PR 11.17 owns the ambient-state decision.
- Record the reversal condition: a measured synchronous hot path with no owning builder and a demonstrated allocation bottleneck.

**Explicitly deferred**

- No `thread_local!`, `RefCell<String>`, temporal parser rewrite, benchmark shape, or production change.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/conc-thread-local.md
```

## Skeleton

```text
Status: superseded by PR 11.6
Conflict resolution: explicit BatchBuilder scratch wins over ambient thread state.
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/conc-thread-local.md
workspace-test = cargo test --workspace
no-thread-local = ! rg -n 'thread_local!' crates tests
evidence-note = test -s docs/implementation/notes/rust-skills/conc-thread-local.md
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The canonical note cites both owning predecessors and the reversal condition.
- [ ] No production, benchmark, manifest, or lockfile changed.
- [ ] The zero-thread-local probe remains clean.

## What completed looks like

The task closes with ownership evidence and no duplicate scratch implementation.

## Hints & gotchas

If PR 11.6's `ts_buf` is missing, block; do not implement this later rule as a substitute.

## References

- Rule: [`conc-thread-local`](../../../.claude/skills/rust-skills/rules/conc-thread-local.md)
- [Roadmap](../README.md) · Prev: PR 15.1 · Next: PR 15.3
