<!--
  Canonical task-file template for the walrus implementation curriculum.
  Copy this file when adding a new PR task. Keep every section — a missing
  "Definition of Done" is the one thing a reviewer will always reject.
-->

# PR 15.4 — Guard the zero-OS-thread invariant and document the async structured-concurrency equivalent

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 15 — Rust concurrency · **Crates touched:** *(none — `.github/` + `docs/`)* ·
> **Est. size:** S · **Depends on:** PR 15.3 · **Unlocks:** PR 16.1

Production has **0 OS-thread creation sites** across 84 non-test `*.rs` files. Two legitimate
`std::thread::available_parallelism` queries size Tokio runtimes, and one sibling test deliberately
uses `std::thread::spawn` to prove `TableDb: Send`; none creates a production thread. There is no
production stack frame whose data a scoped thread could borrow, so `conc-scoped-threads` has nothing
to apply to. Production concurrency is Tokio: **12 `tokio::spawn` sites** and **41
`CancellationToken` references**, which provide part of the async analogue of `thread::scope`'s
join-before-return guarantee. Today nothing stops a production thread spawn from silently eroding
that topology. This PR nails it down with a CI
guard in the existing `fmt · clippy · test` job and writes the note that explains what walrus uses
*instead* — including the reason it could not switch even if it wanted to.

## Why — learning objectives

- **`std::thread::scope` lifetime guarantees vs `Arc` sharing** — and what "structured concurrency"
  means once the runtime is async rather than OS-threaded.
- **Why the loader's `Send + !Sync` DuckDB connection makes borrowed apply futures non-`Send`**, and how
  `CancellationToken` shapes walrus's graceful shutdown.

## Read first

- [`conc-scoped-threads`](../../../.claude/skills/rust-skills/rules/conc-scoped-threads.md) — take
  the *Key Points*: "all threads are joined automatically when the scope returns — even on panic",
  and the `'env` borrow guarantee. Those two properties are exactly what you must show walrus already
  gets from awaited `JoinHandle`s + `CancellationToken`, without OS threads.
- `crates/pg-sink/src/reload.rs:405-485` (`spawn_exporter`) — the `tokio::spawn` at line 424, the
  `OwnedSemaphorePermit` moved *into* the task so the slot frees on every exit path, and the
  `self.token.child_token()` at line 410. This is walrus's structured-concurrency shape: a child
  token scopes the task's lifetime to its parent's.
- `crates/loader/src/health.rs:117-126` and `crates/pg-sink/src/health.rs:155-164` — `serve_on`,
  whose `shutdown: CancellationToken` parameter feeds
  `.with_graceful_shutdown(async move { shutdown.cancelled().await })`. Cancellation, not a thread
  join, is how walrus ends work deterministically.
- `crates/loader/src/apply_loop.rs:1-30` — the module doc ("**one worker per `.duckdb` file** —
  never share a DuckDB connection across tables") and `apply_loop`, the function at line 17 that
  establishes the loader's `future_not_send` exception boundary.
- `.github/workflows/ci.yml:53-104` — the `gates` job (`name: fmt · clippy · test`): the fmt step at
  `:93`, the clippy step at `:96-97`, the test step at `:99`. Your guard goes immediately after
  clippy.
- `docs/implementation/notes/duckdb-lts-bump.md` — the house style for a decision note (H1, a
  `> **Status:**` blockquote stating the verdict, then evidence).

## Baseline contract

- **Precondition:** Confirm `rule-present`, then reproduce the audited non-applicability or rejected
  tradeoff on the immediate predecessor by named path and symbol. Historical line coordinates are
  navigation hints only.
- **Locked outcome:** This is an evidence task. Land only the decision note and mechanical guards
  explicitly listed in the allowlist; do not adopt the rejected optimization or API. The canonical
  artifact is `docs/implementation/notes/rust-skills/conc-scoped-threads.md`.
- **Allowed files:** The **Files to create / modify** block is exhaustive. Any applicable production
  change, ambiguous mapping, or newly required path blocks; do not convert the task to `change`.

## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

1. Create `scripts/check-no-os-threads.sh`, which scans non-test production sources under
   `crates/*/src` and fails with the offending `file:line` on thread creation (`std::thread::spawn`,
   `std::thread::scope`, or `std::thread::Builder`) or a thread-module import that can hide one,
   including `use std::{thread};`. It deliberately permits direct
   `std::thread::available_parallelism`. Its
   `--self-test` mode uses a `mktemp -d` source root to prove clean and both rejected forms without
   editing tracked Rust.
2. Add one guard step to the `gates` job in `.github/workflows/ci.yml`, immediately after the clippy
   step (`ci.yml:96-97`), that runs the script.
3. Write `docs/implementation/notes/rust-skills/conc-scoped-threads.md` — the decision note explaining
   `CancellationToken` + awaited `JoinHandle`s as walrus's structured-concurrency substitute, with
   the positive evidence (not an assertion of inapplicability).

**Conflict — read this before you start.** CONTEXT §4 pre-decides this rule as archetype **D**
("zero rayon/threads in the tree"). Honoured — but with two required qualifications:

- The note **must give positive evidence**: 0 production thread-creation sites across 84 non-test
  source files, 12 `tokio::spawn` sites, and 41 `CancellationToken` references. "The rule does not
  apply" is a failed note.
- The note **must state the type boundary accurately**: `Connection` and `TableDb` are `Send +
  !Sync`, so futures borrowing them are non-`Send`. Owned movement is possible (and tested), but
  requires the ownership redesign recorded by PR 14.18.

**Explicitly deferred** (do *not* build these here)

- **Do not change `tokio::task::JoinSet` usage** — PR 14.13 already adopted it for exporter supervision.
- **Do not convert any blocking DuckDB call to `spawn_blocking`** (0 uses today, also Phase 14).
  Tempting while you are writing about threads; out of scope.
- **Touch no `crates/**/*.rs` file.** This PR is one CI step and one markdown note.
- The guard greps **non-test production sources only** (`crates/*/src`, excluding `*_test.rs`), so
  the existing Send-bound unit test, a future integration test, or a bench may use a thread
  deliberately. Say so in
  the note — a guard whose blast radius is undocumented gets deleted by the first person it blocks.

## Files to create / modify

```
.github/workflows/ci.yml                      # gates job: a "no OS threads" step after clippy (:96-97)
scripts/check-no-os-threads.sh                # production-source guard + isolated --self-test
docs/implementation/notes/rust-skills/conc-scoped-threads.md    # NEW — CancellationToken + awaited JoinHandles as the
                                              #       structured-concurrency substitute; non-Send DuckDB
```

## Skeleton

Archetype D: the "skeleton" is the CI diff plus the note's shape. There is no Rust to write.

```bash
# scripts/check-no-os-threads.sh — operative scan contract (plus argument/self-test plumbing).
# Direct creation paths are banned; thread-module imports are banned because they can hide a later
# `thread::spawn`. Direct `std::thread::available_parallelism` remains allowed for runtime sizing.
thread_pattern='std::thread::(spawn|scope|Builder)|use[[:space:]]+std::thread|use[[:space:]]+std::\{[^}]*\bthread\b'
todo "rg -n --glob '*.rs' --glob '!*_test.rs' \"$thread_pattern\" over the requested fixture root \
      or crates/*/src; any match prints file:line and fails"
todo "--self-test proves clean and available_parallelism fixtures pass, while std::thread::spawn \
      and use std::{thread}; fixtures fail without touching tracked files"
```

```yaml
# .github/workflows/ci.yml — in the `gates` job (name: fmt · clippy · test), immediately AFTER the
# clippy step at :96-97 and BEFORE `cargo test --workspace` at :99. It costs milliseconds, so it
# runs after the two real gates rather than delaying them.

      - name: cargo clippy --all-targets --all-features -- -D warnings
        run: cargo clippy --all-targets --all-features -- -D warnings

      # PR 15.4 — walrus spawns no production OS threads: concurrency is Tokio task topology scoped
      # by CancellationToken. TableDb is Send + !Sync; moving ownership to a dedicated thread would
      # require the explicit PR 14.18 redesign, not an incidental spawn.
      # Non-test production sources only — unit/integration tests and benches may use threads.
      # Rationale: docs/implementation/notes/rust-skills/conc-scoped-threads.md
      - name: No OS threads in production sources
        run: bash scripts/check-no-os-threads.sh

      - name: cargo test --workspace
        run: cargo test --workspace
```

The note follows the house shape used by `docs/implementation/notes/duckdb-lts-bump.md`: an H1
`# No OS threads in walrus (PR 15.4)`, then a verdict blockquote, then the evidence. Its required
outline (each entry is one `##` heading in the new file):

- **Blockquote at the top**, before any heading:
  `> **Status:** decided <date> — **the zero-OS-thread invariant is now a CI gate.**` … `std::thread`
  has no production creation site; concurrency is Tokio tasks scoped by `CancellationToken`, which
  is the async form of the join-before-return guarantee `std::thread::scope` provides.
- **`The evidence`** — the four probes as a table, with these exact results:

  | probe | result |
  |---|---|
  | guard creation pattern over `crates/*/src`, excluding `*_test.rs` | **0** (across 84 `.rs` files) |
  | direct `std::thread::available_parallelism` | **2**, allowed topology queries |
  | sibling `std::thread::spawn` | **1**, PR 14.18's `TableDb: Send` unit test, outside guard scope |
  | `grep -rn --include='*.rs' 'tokio::spawn' crates/*/src \| grep -v _test.rs \| wc -l` | **12** |
  | `grep -rn --include='*.rs' 'CancellationToken' crates/*/src \| grep -v _test.rs \| wc -l` | **41** |

- **`What thread::scope guarantees, and where walrus gets the same thing`** — the mapping.
  `thread::scope` gives (a) children joined before the scope returns, even on panic, and (b) children
  borrowing `'env` stack data. walrus gets (a) from `self.token.child_token()`
  (`crates/pg-sink/src/reload.rs:410`) plus awaited `JoinHandle`s and
  `.with_graceful_shutdown(shutdown.cancelled())` (`crates/loader/src/health.rs:120-123`,
  `crates/pg-sink/src/health.rs:158`). It does **not** get (b): a `tokio::spawn`ed future is
  `'static`, so walrus pays with `move` + `Arc`. Say that gap out loud.
- **`The hard constraint: borrowed DuckDB work is not Send`** — `duckdb::Connection` and `TableDb`
  are `Send + !Sync`, so the apply-loop future holding shared borrows is non-`Send`. PR 14.18's unit
  test proves owned movement across a thread is possible, but production requires an explicit
  ownership/recovery redesign before doing so.
- **`Scope of the guard`** — it greps `crates/*/src` excluding sibling `*_test.rs`. Integration tests,
  unit tests, and `crates/*/benches/` are deliberately not covered; production topology is.
- **`What would reopen this`** — the concrete trigger, e.g. a CPU-bound, order-independent stage that
  profiles as dominant *and* whose data is `Send`. Name the evidence that would suffice.
- **`See also`** — `.claude/skills/rust-skills/rules/conc-scoped-threads.md` and
  `docs/implementation/notes/no-data-parallelism.md` (PR 15.3 — the sibling rayon ban).

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/conc-scoped-threads.md
workspace-test = cargo test --workspace
guard-self-test = bash scripts/check-no-os-threads.sh --self-test
evidence-note = test -s docs/implementation/notes/rust-skills/conc-scoped-threads.md
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] `grep -c 'std::thread' .github/workflows/ci.yml` prints **≥ 1**.
- [ ] The guard step lives in the `gates` job (`name: fmt · clippy · test`), positioned **after** the
  clippy step and **before** `cargo test --workspace`, and carries a comment linking the note.
- [ ] `bash scripts/check-no-os-threads.sh` exits 0 on the checkout, and `--self-test` proves a
  disposable `std::thread::spawn` fixture is rejected with its fixture `file:line`; no tracked
  source is edited.
- [ ] `docs/implementation/notes/rust-skills/conc-scoped-threads.md` exists, opens with a `> **Status:**` blockquote
  stating the verdict, and contains: the four evidence probes with their numbers, the
  `thread::scope` → `CancellationToken`/awaited-`JoinHandle` mapping (including the `'static`
  gap that async does *not* reproduce), the `Send + !Sync` DuckDB boundary and owned-movement test,
  an explicit statement of the guard's scope, and a reopen
  trigger.
- [ ] No new `JoinSet`, no `spawn_blocking` call, and no new dependency; PR 14.13's existing JoinSet
      supervision is unchanged.
- [ ] `git diff --stat origin/main...HEAD -- crates/` is **empty**: no Rust source file is touched.
- [ ] **Green locally and in CI:**
  - [ ] `cargo fmt --check`
  - [ ] `cargo clippy --all-targets --all-features -- -D warnings`
  - [ ] `cargo test --workspace` (no crate is touched here, so `--workspace` is the scope)

## What completed looks like

```console
$ # ---------- BEFORE (main today) ----------
$ rg -n 'std::thread::(spawn|scope|Builder)' crates/*/src --glob '*.rs' --glob '!*_test.rs' | wc -l
       0                      # the production-creation invariant already holds …
$ grep -c 'std::thread' .github/workflows/ci.yml
0                             # … but nothing prevents it regressing
$ ls docs/implementation/notes/
commit-visibility-race.md
duckdb-lts-bump.md
no-data-parallelism.md

$ # ---------- AFTER this PR ----------
$ grep -c 'std::thread' .github/workflows/ci.yml
1
$ grep -n -A2 'No OS threads' .github/workflows/ci.yml
99:      - name: No OS threads in production sources
100:        run: bash scripts/check-no-os-threads.sh

$ # the guard passes on a clean tree
$ bash scripts/check-no-os-threads.sh
check-no-os-threads: PASS

$ bash scripts/check-no-os-threads.sh --self-test
ok: clean temporary source tree passes
ok: temporary `std::thread::spawn` fixture is rejected
check-no-os-threads self-test: PASS

$ ls docs/implementation/notes/
commit-visibility-race.md
duckdb-lts-bump.md
no-data-parallelism.md
conc-scoped-threads.md

$ git diff --stat origin/main...HEAD -- crates/
                              # (empty — no Rust source touched)
```

## Hints & gotchas

- **`! grep …` and `set -e` do interact — correctly, but only if you keep the `!`.** GitHub's default
  shell for `run:` is `bash --noprofile --norc -eo pipefail`. A negated command is a *tested* command,
  so a `grep` that finds nothing (exit 1) is inverted to 0 and does **not** trip `-e`; a `grep` that
  finds a match becomes exit 1 and fails the step. Drop the `!` and you invert the whole gate.
- **The scan pattern is already decided.** It must catch direct creation paths
  (`std::thread::spawn`, `scope`, `Builder`), `use std::thread`, and brace imports such as
  `use std::{thread};`; the self-test also proves direct `available_parallelism` remains allowed.
- **The glob deliberately excludes sibling `*_test.rs` files.** PR 14.18 already uses an OS thread
  in `duck_test.rs` to prove owned `TableDb: Send`. Integration tests and benches are likewise not
  covered. The guarded invariant is production topology, not a ban on testing thread traits.
- **Do not put this in a new job.** The `gates` job already has a warmed cargo cache and runs on
  every push; a separate job would pay checkout + toolchain setup for a `grep`. Note also that the
  `gates` job is gated on `needs.changes.outputs.code == 'true'`, so a docs-only PR skips it — which
  is fine, because a docs-only PR cannot add a thread.
- **`tokio::spawn` is *not* structured concurrency by itself** — a detached task outlives its parent.
  What makes walrus's usage structured is the `child_token()` + the moved `OwnedSemaphorePermit`
  (`crates/pg-sink/src/reload.rs:410,424-425`), which bound the task's lifetime and release its slot
  on every exit path. Get that distinction right in the note; it is the interesting part.
- **Be honest about the one guarantee async does not reproduce.** `thread::scope` lets children
  borrow `'env` stack data; a `tokio::spawn`ed future must be `'static`, so walrus pays with `move`
  + `Arc` (see the `Arc<AtomicI64>` handoff PR 15.1 just retuned). Writing "async gives you
  everything scoped threads give you" would be wrong.
- Keep prose hard-wrapped at ~95–100 columns to match the rest of `docs/implementation/notes/`.
- No `.sql` file and no `.sqlx` cache entry is involved — and must not be, since there is no Docker
  on this machine to regenerate the offline cache.

## References

- Rule: [`conc-scoped-threads`](../../../.claude/skills/rust-skills/rules/conc-scoped-threads.md)
- Design: `docs/walrus-loader.md` §8.4 (one worker per `.duckdb` file) and `docs/architecture.md`
  (graceful shutdown / cancellation).
- Prev: [PR 15.3](./pr-15.3-conc-rayon-par-iter.md) · Next: [PR 16.1](../phase-16-rust-codegen-opt/pr-16.1-opt-lto-release.md) *(phase boundary → Phase 16 Rust compiler optimization)* · [Roadmap](../README.md)
