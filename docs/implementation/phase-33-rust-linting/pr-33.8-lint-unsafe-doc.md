<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 33.8 — Record why zero-unsafe policy needs no extra doc lint

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 33 — Rust linting · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 33.7 · **Unlocks:** PR 33.9

Apply [`lint-unsafe-doc`](../../../.claude/skills/rust-skills/rules/lint-unsafe-doc.md) to Walrus with the
predetermined disposition above. The rule is: **Require documentation for unsafe blocks**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `lint-unsafe-doc` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Phase 12 establishes a zero-unsafe first-party tree and safety policy; there is no unsafe site for an additional documentation lint to diagnose.

## Read first

- [`lint-unsafe-doc`](../../../.claude/skills/rust-skills/rules/lint-unsafe-doc.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Record the zero-site audit and the exact Phase 12 tripwire/lint that would make future unsafe require documentation.
- Prove this postcondition: The note proves both unsafe functions and blocks are absent and names the reversal condition.

**Explicitly deferred** (do not build these here)

- Do not enable a redundant lint with no first-party target or inspect dependency source.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '\bunsafe\b|missing_safety_doc|undocumented_unsafe_blocks' Cargo.toml crates --glob '*.toml' --glob '*.rs' || true
```

Phase 12 establishes a zero-unsafe first-party tree and safety policy; there is no unsafe site for an additional documentation lint to diagnose.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/lint-unsafe-doc.md` only.

**Required action:** Record the zero-site audit and the exact Phase 12 tripwire/lint that would make future unsafe require documentation.

**Acceptance:** The note proves both unsafe functions and blocks are absent and names the reversal condition.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/lint-unsafe-doc.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/lint-unsafe-doc.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/lint-unsafe-doc.md` only.
operation = Record the zero-site audit and the exact Phase 12 tripwire/lint that would make future unsafe require documentation.
proof = The note proves both unsafe functions and blocks are absent and names the reversal condition.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = ! rg -n '\bunsafe\b|missing_safety_doc|undocumented_unsafe_blocks' Cargo.toml crates --glob '*.toml' --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note proves both unsafe functions and blocks are absent and names the reversal condition.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=33.8
rule=lint-unsafe-doc
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/lint-unsafe-doc.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `lint-unsafe-doc`](../../../.claude/skills/rust-skills/rules/lint-unsafe-doc.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
