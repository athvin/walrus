<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 33.2 — Record PR 33.1 as the owner of `clippy::suspicious` enforcement

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** superseded by PR 33.1
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 33 — Rust linting · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 33.1 · **Unlocks:** PR 33.3

Apply [`lint-warn-suspicious`](../../../.claude/skills/rust-skills/rules/lint-warn-suspicious.md) to Walrus with the
predetermined disposition above. The rule is: **Enable clippy::suspicious for likely bugs**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `lint-warn-suspicious` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Clippy all already contains suspicious and is denied workspace-wide; PR 33.1 records that enforcement.

## Read first

- [`lint-warn-suspicious`](../../../.claude/skills/rust-skills/rules/lint-warn-suspicious.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Record the group membership, PR 33.1 policy, and clean full Clippy command.
- Prove this postcondition: The note finds no narrower allow for suspicious and no member missing lint inheritance.

**Explicitly deferred** (do not build these here)

- Do not downgrade an already denied group to warn or duplicate configuration.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
cargo clippy --workspace --all-targets --all-features -- -D warnings
```

Clippy all already contains suspicious and is denied workspace-wide; PR 33.1 records that enforcement.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/lint-warn-suspicious.md` only.

**Required action:** Record the group membership, PR 33.1 policy, and clean full Clippy command.

**Acceptance:** The note finds no narrower allow for suspicious and no member missing lint inheritance.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/lint-warn-suspicious.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/lint-warn-suspicious.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/lint-warn-suspicious.md` only.
operation = Record the group membership, PR 33.1 policy, and clean full Clippy command.
proof = The note finds no narrower allow for suspicious and no member missing lint inheritance.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = cargo clippy --workspace --all-targets --all-features -- -D warnings
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note finds no narrower allow for suspicious and no member missing lint inheritance.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=33.2
rule=lint-warn-suspicious
outcome=superseded by PR 33.1
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/lint-warn-suspicious.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `lint-warn-suspicious`](../../../.claude/skills/rust-skills/rules/lint-warn-suspicious.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
