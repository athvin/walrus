<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 28.10 — Property-test `Lsn` round trips and textual ordering

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
>
> **Gates:** fmt,clippy,test · **Test packages:** common

> **Phase:** 28 — Rust testing · **Crates touched:** `common` · **Est. size:** M ·
> **Depends on:** PR 28.9 · **Unlocks:** PR 28.11

Apply [`test-proptest-properties`](../../../.claude/skills/rust-skills/rules/test-proptest-properties.md) to Walrus with the
predetermined disposition above. The rule is: **Use proptest for property-based testing**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `test-proptest-properties` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: `Lsn` has load-bearing parse/display and lexical-order invariants currently covered only by examples; it is a bounded, deterministic property-test target.

## Read first

- [`test-proptest-properties`](../../../.claude/skills/rust-skills/rules/test-proptest-properties.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Add proptest as a workspace dev-dependency and generate arbitrary `u64` LSNs to prove display/parse round-trip and that fixed-width textual ordering equals numeric ordering; configure explicit case count without persistence files.
- Prove this postcondition: Both properties run under the ordinary common test target, report a reproducible seed on failure, and no production dependency gains proptest.

**Explicitly deferred** (do not build these here)

- Do not property-test SQL, external services, time, or already exhaustive small enums.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'proptest|lsn.*(round|order)|numeric.*text' Cargo.toml crates/common --glob '*.toml' --glob '*.rs' || true
```

`Lsn` has load-bearing parse/display and lexical-order invariants currently covered only by examples; it is a bounded, deterministic property-test target.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

## Implementation contract

**Allowed files:** `Cargo.toml`, `crates/common/Cargo.toml`, and `crates/common/src/lsn_test.rs`.

**Required action:** Add proptest as a workspace dev-dependency and generate arbitrary `u64` LSNs to prove display/parse round-trip and that fixed-width textual ordering equals numeric ordering; configure explicit case count without persistence files.

**Acceptance:** Both properties run under the ordinary common test target, report a reproducible seed on failure, and no production dependency gains proptest.

**Baseline mismatch:** If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

**Evidence note schema:** No evidence note is authorized by this change ticket. A baseline mismatch requires re-auditing and re-authoring the task before it can be selected again.

## Files to create / modify

```text
Cargo.toml
crates/common/Cargo.toml
crates/common/src/lsn_test.rs
```

## Skeleton

```text
target = `Cargo.toml`, `crates/common/Cargo.toml`, and `crates/common/src/lsn_test.rs`.
operation = Add proptest as a workspace dev-dependency and generate arbitrary `u64` LSNs to prove display/parse round-trip and that fixed-width textual ordering equals numeric ordering; configure explicit case count without persistence files.
proof = Both properties run under the ordinary common test target, report a reproducible seed on failure, and no production dependency gains proptest.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'proptest|lsn.*(round|order)|numeric.*text' Cargo.toml crates/common --glob '*.toml' --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test -p common
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] Both properties run under the ordinary common test target, report a reproducible seed on failure, and no production dependency gains proptest.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=28.10
rule=test-proptest-properties
outcome=change
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=none
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `test-proptest-properties`](../../../.claude/skills/rust-skills/rules/test-proptest-properties.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
