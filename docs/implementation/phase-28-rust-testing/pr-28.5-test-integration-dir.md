<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 28.5 — Verify crate-root integration-test placement

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 28 — Rust testing · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 28.4 · **Unlocks:** PR 28.6

Apply [`test-integration-dir`](../../../.claude/skills/rust-skills/rules/test-integration-dir.md) to Walrus with the
predetermined disposition above. The rule is: **Put integration tests in the `tests/` directory**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `test-integration-dir` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Walrus already keeps cross-component and public-surface tests in crate-root `tests/` directories; sibling `src/*_test.rs` files are private unit tests by design.

## Read first

- [`test-integration-dir`](../../../.claude/skills/rust-skills/rules/test-integration-dir.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Record the integration-test inventory by package, confirm every listed file is outside `src/`, and distinguish it from the sibling-unit-test convention established by PR 28.1.
- Prove this postcondition: The note contains the exact inventory command, package counts, the unit/integration boundary, and a reversal condition for any cross-component test added under `src/`.

**Explicitly deferred** (do not build these here)

- Do not relocate tests merely to demonstrate the rule or duplicate an integration scenario.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
find crates tests -path '*/tests/*.rs' -type f | sort
```

Walrus already keeps cross-component and public-surface tests in crate-root `tests/` directories; sibling `src/*_test.rs` files are private unit tests by design.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/test-integration-dir.md` only.

**Required action:** Record the integration-test inventory by package, confirm every listed file is outside `src/`, and distinguish it from the sibling-unit-test convention established by PR 28.1.

**Acceptance:** The note contains the exact inventory command, package counts, the unit/integration boundary, and a reversal condition for any cross-component test added under `src/`.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/test-integration-dir.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/test-integration-dir.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/test-integration-dir.md` only.
operation = Record the integration-test inventory by package, confirm every listed file is outside `src/`, and distinguish it from the sibling-unit-test convention established by PR 28.1.
proof = The note contains the exact inventory command, package counts, the unit/integration boundary, and a reversal condition for any cross-component test added under `src/`.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = find crates tests -path '*/tests/*.rs' -type f
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note contains the exact inventory command, package counts, the unit/integration boundary, and a reversal condition for any cross-component test added under `src/`.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=28.5
rule=test-integration-dir
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/test-integration-dir.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `test-integration-dir`](../../../.claude/skills/rust-skills/rules/test-integration-dir.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
