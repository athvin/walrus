<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 32.9 — Verify one named binary per service package

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 32 — Rust project structure · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 32.8 · **Unlocks:** PR 32.10

Apply [`proj-bin-dir`](../../../.claude/skills/rust-skills/rules/proj-bin-dir.md) to Walrus with the
predetermined disposition above. The rule is: **Put multiple binaries in src/bin/**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `proj-bin-dir` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Each service package owns exactly one explicitly named binary; `src/bin/` is useful only when one package owns multiple binaries.

## Read first

- [`proj-bin-dir`](../../../.claude/skills/rust-skills/rules/proj-bin-dir.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Inventory binary targets by package and record their manifest paths and product ownership.
- Prove this postcondition: The note confirms one binary per service package and states a second binary in either package as the reversal condition.

**Explicitly deferred** (do not build these here)

- Do not move a single main into src/bin or merge both services into one package.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
find crates -path '*/src/main.rs' -o -path '*/src/bin/*.rs' | sort && rg -n '^\[\[bin\]\]|^name = \"walrus-' crates/*/Cargo.toml
```

Each service package owns exactly one explicitly named binary; `src/bin/` is useful only when one package owns multiple binaries.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/proj-bin-dir.md` only.

**Required action:** Inventory binary targets by package and record their manifest paths and product ownership.

**Acceptance:** The note confirms one binary per service package and states a second binary in either package as the reversal condition.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/proj-bin-dir.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/proj-bin-dir.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/proj-bin-dir.md` only.
operation = Inventory binary targets by package and record their manifest paths and product ownership.
proof = The note confirms one binary per service package and states a second binary in either package as the reversal condition.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = find crates -path '*/src/main.rs' -o -path '*/src/bin/*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note confirms one binary per service package and states a second binary in either package as the reversal condition.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=32.9
rule=proj-bin-dir
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/proj-bin-dir.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `proj-bin-dir`](../../../.claude/skills/rust-skills/rules/proj-bin-dir.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
