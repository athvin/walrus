<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 32.14 — Record the absence of first-party build scripts

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 32 — Rust project structure · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 32.13 · **Unlocks:** PR 33.1

Apply [`proj-build-rs-minimal`](../../../.claude/skills/rust-skills/rules/proj-build-rs-minimal.md) to Walrus with the
predetermined disposition above. The rule is: **Keep `build.rs` minimal, deterministic, and idempotent**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `proj-build-rs-minimal` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Walrus has no first-party build script; native dependency build scripts are outside repository policy and cannot be rewritten here.

## Read first

- [`proj-build-rs-minimal`](../../../.claude/skills/rust-skills/rules/proj-build-rs-minimal.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Record the zero first-party result and distinguish Cargo registry/git dependency build scripts from workspace sources.
- Prove this postcondition: The note states that adding a first-party build.rs triggers deterministic inputs, rerun-if directives, and hermetic-output review.

**Explicitly deferred** (do not build these here)

- Do not add a build script or vendor dependency build logic.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
find . -name build.rs -not -path './target/*' -print | sort
```

Walrus has no first-party build script; native dependency build scripts are outside repository policy and cannot be rewritten here.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/proj-build-rs-minimal.md` only.

**Required action:** Record the zero first-party result and distinguish Cargo registry/git dependency build scripts from workspace sources.

**Acceptance:** The note states that adding a first-party build.rs triggers deterministic inputs, rerun-if directives, and hermetic-output review.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/proj-build-rs-minimal.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/proj-build-rs-minimal.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/proj-build-rs-minimal.md` only.
operation = Record the zero first-party result and distinguish Cargo registry/git dependency build scripts from workspace sources.
proof = The note states that adding a first-party build.rs triggers deterministic inputs, rerun-if directives, and hermetic-output review.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = ! rg -n 'fn main\(' . --glob build.rs --glob '!target/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note states that adding a first-party build.rs triggers deterministic inputs, rerun-if directives, and hermetic-output review.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=32.14
rule=proj-build-rs-minimal
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/proj-build-rs-minimal.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `proj-build-rs-minimal`](../../../.claude/skills/rust-skills/rules/proj-build-rs-minimal.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
