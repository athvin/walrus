<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 32.3 — Record why the current per-crate module depth stays flat

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 32 — Rust project structure · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 32.2 · **Unlocks:** PR 32.4

Apply [`proj-flat-small`](../../../.claude/skills/rust-skills/rules/proj-flat-small.md) to Walrus with the
predetermined disposition above. The rule is: **Keep small projects flat**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `proj-flat-small` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Walrus is not a small single-crate project; its mostly flat per-crate trees already balance discoverability with one cohesive pgoutput submodule.

## Read first

- [`proj-flat-small`](../../../.claude/skills/rust-skills/rules/proj-flat-small.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Record module depth and file counts per crate and explain why further flattening would cross package/capability boundaries.
- Prove this postcondition: The note documents the only nested production module and its cohesion.

**Explicitly deferred** (do not build these here)

- Do not collapse the workspace or move pgoutput files to satisfy a small-project heuristic.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
find crates -path '*/src/*.rs' -type f | awk -F/ '{print NF, $0}' | sort -n
```

Walrus is not a small single-crate project; its mostly flat per-crate trees already balance discoverability with one cohesive pgoutput submodule.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/proj-flat-small.md` only.

**Required action:** Record module depth and file counts per crate and explain why further flattening would cross package/capability boundaries.

**Acceptance:** The note documents the only nested production module and its cohesion.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/proj-flat-small.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/proj-flat-small.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/proj-flat-small.md` only.
operation = Record module depth and file counts per crate and explain why further flattening would cross package/capability boundaries.
proof = The note documents the only nested production module and its cohesion.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = find crates -path '*/src/*.rs' -type f
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note documents the only nested production module and its cohesion.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=32.3
rule=proj-flat-small
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/proj-flat-small.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `proj-flat-small`](../../../.claude/skills/rust-skills/rules/proj-flat-small.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
