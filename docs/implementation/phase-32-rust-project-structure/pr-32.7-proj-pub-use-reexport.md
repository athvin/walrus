<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 32.7 — Verify current re-exports form the intended public facades

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 32 — Rust project structure · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 32.6 · **Unlocks:** PR 32.8

Apply [`proj-pub-use-reexport`](../../../.claude/skills/rust-skills/rules/proj-pub-use-reexport.md) to Walrus with the
predetermined disposition above. The rule is: **Use pub use for clean public API**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `proj-pub-use-reexport` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Common, control, and pg-to-arrow already re-export their intended convenience surfaces; loader and pg-sink retain module-qualified service APIs to avoid collisions and hide no stable façade.

## Read first

- [`proj-pub-use-reexport`](../../../.claude/skills/rust-skills/rules/proj-pub-use-reexport.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Inventory current reexports, their downstream use, and duplicate type/function names that make wildcard flattening undesirable.
- Prove this postcondition: The note distinguishes convenience façades from module-qualified integration seams and identifies an actual repeated downstream import as the reversal condition.

**Explicitly deferred** (do not build these here)

- Do not re-export every public module item or introduce ambiguous root names.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '^pub use|^pub mod' crates/*/src/lib.rs || true
```

Common, control, and pg-to-arrow already re-export their intended convenience surfaces; loader and pg-sink retain module-qualified service APIs to avoid collisions and hide no stable façade.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/proj-pub-use-reexport.md` only.

**Required action:** Inventory current reexports, their downstream use, and duplicate type/function names that make wildcard flattening undesirable.

**Acceptance:** The note distinguishes convenience façades from module-qualified integration seams and identifies an actual repeated downstream import as the reversal condition.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/proj-pub-use-reexport.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/proj-pub-use-reexport.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/proj-pub-use-reexport.md` only.
operation = Inventory current reexports, their downstream use, and duplicate type/function names that make wildcard flattening undesirable.
proof = The note distinguishes convenience façades from module-qualified integration seams and identifies an actual repeated downstream import as the reversal condition.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n '^pub use|^pub mod' crates/*/src/lib.rs
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note distinguishes convenience façades from module-qualified integration seams and identifies an actual repeated downstream import as the reversal condition.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=32.7
rule=proj-pub-use-reexport
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/proj-pub-use-reexport.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `proj-pub-use-reexport`](../../../.claude/skills/rust-skills/rules/proj-pub-use-reexport.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
