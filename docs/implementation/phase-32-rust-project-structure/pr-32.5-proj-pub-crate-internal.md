<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 32.5 — Verify public visibility has external consumers

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 32 — Rust project structure · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 32.4 · **Unlocks:** PR 32.6

Apply [`proj-pub-crate-internal`](../../../.claude/skills/rust-skills/rules/proj-pub-crate-internal.md) to Walrus with the
predetermined disposition above. The rule is: **Use pub(crate) for internal APIs**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `proj-pub-crate-internal` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Broad visibility is primarily required by crate-root integration tests and cross-crate service composition; known private helpers already use pub(crate) or private visibility.

## Read first

- [`proj-pub-crate-internal`](../../../.claude/skills/rust-skills/rules/proj-pub-crate-internal.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: For each public module root, identify its external consumer in another package or integration test; record any unconsumed item as a blocking contradiction rather than choosing a replacement API.
- Prove this postcondition: The note classifies the reachable surface and confirms `cargo test --all-targets` needs the retained integration seams.

**Explicitly deferred** (do not build these here)

- Do not narrow visibility until the owning integration test/public consumer is migrated by a dedicated API task.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '^\s*pub(\(crate\))? (async )?(fn|struct|enum|trait|type|mod)' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' || true
```

Broad visibility is primarily required by crate-root integration tests and cross-crate service composition; known private helpers already use pub(crate) or private visibility.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/proj-pub-crate-internal.md` only.

**Required action:** For each public module root, identify its external consumer in another package or integration test; record any unconsumed item as a blocking contradiction rather than choosing a replacement API.

**Acceptance:** The note classifies the reachable surface and confirms `cargo test --all-targets` needs the retained integration seams.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/proj-pub-crate-internal.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/proj-pub-crate-internal.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/proj-pub-crate-internal.md` only.
operation = For each public module root, identify its external consumer in another package or integration test; record any unconsumed item as a blocking contradiction rather than choosing a replacement API.
proof = The note classifies the reachable surface and confirms `cargo test --all-targets` needs the retained integration seams.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n '^\s*pub(\(crate\))? (async )?(fn|struct|enum|trait|type|mod)' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note classifies the reachable surface and confirms `cargo test --all-targets` needs the retained integration seams.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=32.5
rule=proj-pub-crate-internal
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/proj-pub-crate-internal.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `proj-pub-crate-internal`](../../../.claude/skills/rust-skills/rules/proj-pub-crate-internal.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
