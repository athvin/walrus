<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 32.2 — Verify top-level modules follow product capabilities

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 32 — Rust project structure · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 32.1 · **Unlocks:** PR 32.3

Apply [`proj-mod-by-feature`](../../../.claude/skills/rust-skills/rules/proj-mod-by-feature.md) to Walrus with the
predetermined disposition above. The rule is: **Organize modules by feature, not type**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `proj-mod-by-feature` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Modules are already named for CDC/control/loader capabilities such as replication, reload, checkpoint, transform, and health rather than generic models/services/utilities buckets.

## Read first

- [`proj-mod-by-feature`](../../../.claude/skills/rust-skills/rules/proj-mod-by-feature.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Inventory each crate's top-level modules and map them to one product capability; call out shared primitive modules in common as the intentional exception.
- Prove this postcondition: The note finds no type-layer bucket requiring a move and gives a reversal condition for a future generic `models`/`utils` module.

**Explicitly deferred** (do not build these here)

- Do not rename modules or churn import paths for taxonomy alone.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
find crates -path '*/src/*.rs' -type f ! -name '*_test.rs' -print | sort
```

Modules are already named for CDC/control/loader capabilities such as replication, reload, checkpoint, transform, and health rather than generic models/services/utilities buckets.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/proj-mod-by-feature.md` only.

**Required action:** Inventory each crate's top-level modules and map them to one product capability; call out shared primitive modules in common as the intentional exception.

**Acceptance:** The note finds no type-layer bucket requiring a move and gives a reversal condition for a future generic `models`/`utils` module.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/proj-mod-by-feature.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/proj-mod-by-feature.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/proj-mod-by-feature.md` only.
operation = Inventory each crate's top-level modules and map them to one product capability; call out shared primitive modules in common as the intentional exception.
proof = The note finds no type-layer bucket requiring a move and gives a reversal condition for a future generic `models`/`utils` module.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = find crates -path '*/src/*.rs' -type f ! -name '*_test.rs' -print
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note finds no type-layer bucket requiring a move and gives a reversal condition for a future generic `models`/`utils` module.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=32.2
rule=proj-mod-by-feature
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/proj-mod-by-feature.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `proj-mod-by-feature`](../../../.claude/skills/rust-skills/rules/proj-mod-by-feature.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
