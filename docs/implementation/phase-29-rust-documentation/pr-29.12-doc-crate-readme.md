<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 29.12 — Record why monorepo crates must not include the root README

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 29 — Rust documentation · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 29.11 · **Unlocks:** PR 30.1

Apply [`doc-crate-readme`](../../../.claude/skills/rust-skills/rules/doc-crate-readme.md) to Walrus with the
predetermined disposition above. The rule is: **Unify the README and crate root docs with `#![doc = include_str!("../README.md")]`**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `doc-crate-readme` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: The internal crates are components of one deployed product, not independently published libraries; the root README and crate module docs serve different audiences and cannot be safely unified with include_str.

## Read first

- [`doc-crate-readme`](../../../.claude/skills/rust-skills/rules/doc-crate-readme.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Record the absence of per-crate READMEs, identify root README sections whose relative links would be wrong under rustdoc, and define publication as the reversal condition.
- Prove this postcondition: The note demonstrates why `#![doc = include_str!(...)]` would duplicate product/operator material in internal API docs.

**Explicitly deferred** (do not build these here)

- Do not create five placeholder READMEs or include the repository README from every crate.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
find crates tests/e2e -maxdepth 2 -name README.md -print | sort
```

The internal crates are components of one deployed product, not independently published libraries; the root README and crate module docs serve different audiences and cannot be safely unified with include_str.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/doc-crate-readme.md` only.

**Required action:** Record the absence of per-crate READMEs, identify root README sections whose relative links would be wrong under rustdoc, and define publication as the reversal condition.

**Acceptance:** The note demonstrates why `#![doc = include_str!(...)]` would duplicate product/operator material in internal API docs.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/doc-crate-readme.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/doc-crate-readme.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/doc-crate-readme.md` only.
operation = Record the absence of per-crate READMEs, identify root README sections whose relative links would be wrong under rustdoc, and define publication as the reversal condition.
proof = The note demonstrates why `#![doc = include_str!(...)]` would duplicate product/operator material in internal API docs.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = ! rg -n '^readme\s*=' crates/common/Cargo.toml crates/control/Cargo.toml crates/loader/Cargo.toml crates/pg-sink/Cargo.toml crates/pg-to-arrow/Cargo.toml
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note demonstrates why `#![doc = include_str!(...)]` would duplicate product/operator material in internal API docs.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=29.12
rule=doc-crate-readme
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/doc-crate-readme.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `doc-crate-readme`](../../../.claude/skills/rust-skills/rules/doc-crate-readme.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
