<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 29.9 — Record why PR 29.8 examples need no hidden setup

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 29 — Rust documentation · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 29.8 · **Unlocks:** PR 29.10

Apply [`doc-hidden-setup`](../../../.claude/skills/rust-skills/rules/doc-hidden-setup.md) to Walrus with the
predetermined disposition above. The rule is: **Use `# ` prefix to hide example setup code**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `doc-hidden-setup` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: The PR 29.8 examples target small pure APIs and require no distracting fixture setup; hidden setup would obscure rather than clarify them.

## Read first

- [`doc-hidden-setup`](../../../.claude/skills/rust-skills/rules/doc-hidden-setup.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Review every executable example introduced by PR 29.8, record its visible line count and imports, and explain why no line is incidental enough to hide.
- Prove this postcondition: The note lists each example and states the threshold for revisiting the decision when a future example needs multi-line fixture construction.

**Explicitly deferred** (do not build these here)

- Do not hide the value construction or assertion that teaches the API.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '^\s*/// # |^\s*//! # ' crates --glob '*.rs' || true
```

The PR 29.8 examples target small pure APIs and require no distracting fixture setup; hidden setup would obscure rather than clarify them.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/doc-hidden-setup.md` only.

**Required action:** Review every executable example introduced by PR 29.8, record its visible line count and imports, and explain why no line is incidental enough to hide.

**Acceptance:** The note lists each example and states the threshold for revisiting the decision when a future example needs multi-line fixture construction.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/doc-hidden-setup.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/doc-hidden-setup.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/doc-hidden-setup.md` only.
operation = Review every executable example introduced by PR 29.8, record its visible line count and imports, and explain why no line is incidental enough to hide.
proof = The note lists each example and states the threshold for revisiting the decision when a future example needs multi-line fixture construction.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = ! rg -n '^\s*/// # |^\s*//! # ' crates --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note lists each example and states the threshold for revisiting the decision when a future example needs multi-line fixture construction.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=29.9
rule=doc-hidden-setup
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/doc-hidden-setup.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `doc-hidden-setup`](../../../.claude/skills/rust-skills/rules/doc-hidden-setup.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
