<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 29.8 — Add runnable examples for pure public APIs

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
>
> **Gates:** fmt,clippy,test · **Test packages:** common,pg-to-arrow

> **Phase:** 29 — Rust documentation · **Crates touched:** `common`, `pg-to-arrow` · **Est. size:** M ·
> **Depends on:** PR 29.7 · **Unlocks:** PR 29.9

Apply [`doc-examples-section`](../../../.claude/skills/rust-skills/rules/doc-examples-section.md) to Walrus with the
predetermined disposition above. The rule is: **Include `# Examples` with runnable code**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `doc-examples-section` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: The listed pure parsers and value helpers are stable, hermetic entry points where executable examples improve the API contract without starting services.

## Read first

- [`doc-examples-section`](../../../.claude/skills/rust-skills/rules/doc-examples-section.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Add one runnable `# Examples` block per listed API, use Result-returning doctests for fallible calls, and assert a representative value or round trip.
- Prove this postcondition: `cargo test --doc -p common -p pg-to-arrow` runs every new example with no ignored blocks.

**Explicitly deferred** (do not build these here)

- Do not add examples to service/database APIs that cannot be hermetic or duplicate examples on aliases.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '^pub (struct Lsn|fn sql_literal)|^\s*pub fn (parse_rfc3339|parse_interval|parse_timetz|parse_uuid_bytes|parse_range)' crates/{common,pg-to-arrow}/src --glob '*.rs' || true
```

The listed pure parsers and value helpers are stable, hermetic entry points where executable examples improve the API contract without starting services.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

## Implementation contract

**Allowed files:** `crates/common/src/lsn.rs`, `crates/common/src/sql.rs`, `crates/common/src/sink_meta.rs`, `crates/pg-to-arrow/src/tier2.rs`, `crates/pg-to-arrow/src/uuid_enum.rs`, and `crates/pg-to-arrow/src/range.rs`.

**Required action:** Add one runnable `# Examples` block per listed API, use Result-returning doctests for fallible calls, and assert a representative value or round trip.

**Acceptance:** `cargo test --doc -p common -p pg-to-arrow` runs every new example with no ignored blocks.

**Baseline mismatch:** If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

**Evidence note schema:** No evidence note is authorized by this change ticket. A baseline mismatch requires re-auditing and re-authoring the task before it can be selected again.

## Files to create / modify

```text
crates/common/src/lsn.rs
crates/common/src/sql.rs
crates/common/src/sink_meta.rs
crates/pg-to-arrow/src/tier2.rs
crates/pg-to-arrow/src/uuid_enum.rs
crates/pg-to-arrow/src/range.rs
```

## Skeleton

```text
target = `crates/common/src/lsn.rs`, `crates/common/src/sql.rs`, `crates/common/src/sink_meta.rs`, `crates/pg-to-arrow/src/tier2.rs`, `crates/pg-to-arrow/src/uuid_enum.rs`, and `crates/pg-to-arrow/src/range.rs`.
operation = Add one runnable `# Examples` block per listed API, use Result-returning doctests for fallible calls, and assert a representative value or round trip.
proof = `cargo test --doc -p common -p pg-to-arrow` runs every new example with no ignored blocks.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n '^pub (struct Lsn|fn sql_literal)|^\s*pub fn (parse_rfc3339|parse_interval|parse_timetz|parse_uuid_bytes|parse_range)' crates/{common,pg-to-arrow}/src --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test -p common -p pg-to-arrow
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] `cargo test --doc -p common -p pg-to-arrow` runs every new example with no ignored blocks.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=29.8
rule=doc-examples-section
outcome=change
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=none
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `doc-examples-section`](../../../.claude/skills/rust-skills/rules/doc-examples-section.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
