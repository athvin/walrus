<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 29.11 — Mark internal workspace crates as non-publishable

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 29 — Rust documentation · **Crates touched:** none (workspace tooling, manifests, or documentation) · **Est. size:** M ·
> **Depends on:** PR 29.10 · **Unlocks:** PR 29.12

Apply [`doc-cargo-metadata`](../../../.claude/skills/rust-skills/rules/doc-cargo-metadata.md) to Walrus with the
predetermined disposition above. The rule is: **Fill `Cargo.toml` metadata for published crates**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `doc-cargo-metadata` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Walrus is deployed as a monorepo application and none of its internal crates has a crates.io publishing contract, but five member manifests still inherit Cargo's publishable default.

## Read first

- [`doc-cargo-metadata`](../../../.claude/skills/rust-skills/rules/doc-cargo-metadata.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Set `publish = false` in `[workspace.package]` and add `publish.workspace = true` to the five crate packages; retain e2e's existing explicit false setting.
- Prove this postcondition: `cargo metadata --no-deps` reports `publish: []` for all six members and no invented crates.io metadata is added.

**Explicitly deferred** (do not build these here)

- Do not invent keywords, homepage, authors, repository URLs, or docs.rs links.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
for f in crates/*/Cargo.toml tests/e2e/Cargo.toml; do printf '%s ' \"$f\"; rg -n '^(name|version|license|rust-version|publish|description|repository|readme)\s*=' \"$f\" || true; done
```

Walrus is deployed as a monorepo application and none of its internal crates has a crates.io publishing contract, but five member manifests still inherit Cargo's publishable default.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

## Implementation contract

**Allowed files:** `Cargo.toml` and `crates/{common,control,loader,pg-sink,pg-to-arrow}/Cargo.toml`.

**Required action:** Set `publish = false` in `[workspace.package]` and add `publish.workspace = true` to the five crate packages; retain e2e's existing explicit false setting.

**Acceptance:** `cargo metadata --no-deps` reports `publish: []` for all six members and no invented crates.io metadata is added.

**Baseline mismatch:** If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

**Evidence note schema:** No evidence note is authorized by this change ticket. A baseline mismatch requires re-auditing and re-authoring the task before it can be selected again.

## Files to create / modify

```text
Cargo.toml
crates/{common,control,loader,pg-sink,pg-to-arrow}/Cargo.toml
```

## Skeleton

```text
target = `Cargo.toml` and `crates/{common,control,loader,pg-sink,pg-to-arrow}/Cargo.toml`.
operation = Set `publish = false` in `[workspace.package]` and add `publish.workspace = true` to the five crate packages; retain e2e's existing explicit false setting.
proof = `cargo metadata --no-deps` reports `publish: []` for all six members and no invented crates.io metadata is added.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = cargo metadata --no-deps --format-version 1
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] `cargo metadata --no-deps` reports `publish: []` for all six members and no invented crates.io metadata is added.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=29.11
rule=doc-cargo-metadata
outcome=change
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=none
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `doc-cargo-metadata`](../../../.claude/skills/rust-skills/rules/doc-cargo-metadata.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
