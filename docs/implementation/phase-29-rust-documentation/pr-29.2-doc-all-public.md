<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 29.2 — Document every reachable public API

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
>
> **Gates:** fmt,clippy,test · **Test packages:** common,control,loader,pg-sink,pg-to-arrow,e2e

> **Phase:** 29 — Rust documentation · **Crates touched:** `common`, `control`, `loader`, `pg-sink`, `pg-to-arrow`, `e2e` · **Est. size:** L ·
> **Depends on:** PR 29.1 · **Unlocks:** PR 29.3

Apply [`doc-all-public`](../../../.claude/skills/rust-skills/rules/doc-all-public.md) to Walrus with the
predetermined disposition above. The rule is: **Document all public items with `///` doc comments**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `doc-all-public` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: The service libraries intentionally expose integration seams, but many reachable items and fields still lack API-contract documentation.

## Read first

- [`doc-all-public`](../../../.claude/skills/rust-skills/rules/doc-all-public.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Document every item reachable from the five library roots, including public fields and enum variants; describe invariants, units, ownership, and side effects, not implementation syntax.
- Prove this postcondition: `RUSTDOCFLAGS='-D missing_docs -D rustdoc::broken_intra_doc_links' cargo doc --workspace --no-deps` succeeds after excluding binaries only through rustdoc's normal reachability rules.

**Explicitly deferred** (do not build these here)

- Do not widen/narrow visibility, add examples, or enable the permanent workspace lint before PR 33.7.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '^\s*pub (async )?(unsafe )?(fn|struct|enum|trait|type|mod|const|static)|^\s*pub [A-Za-z_][A-Za-z0-9_]*\s*:' crates tests/e2e/src --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' || true
```

The service libraries intentionally expose integration seams, but many reachable items and fields still lack API-contract documentation.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

## Implementation contract

**Allowed files:** Production Rust files under `crates/{common,control,loader,pg-sink,pg-to-arrow}/src` and `tests/e2e/src` only.

**Required action:** Document every item reachable from the five library roots, including public fields and enum variants; describe invariants, units, ownership, and side effects, not implementation syntax.

**Acceptance:** `RUSTDOCFLAGS='-D missing_docs -D rustdoc::broken_intra_doc_links' cargo doc --workspace --no-deps` succeeds after excluding binaries only through rustdoc's normal reachability rules.

**Baseline mismatch:** If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

**Evidence note schema:** No evidence note is authorized by this change ticket. A baseline mismatch requires re-auditing and re-authoring the task before it can be selected again.

## Files to create / modify

```text
crates/{common,control,loader,pg-sink,pg-to-arrow}/src
tests/e2e/src
```

## Skeleton

```text
target = Production Rust files under `crates/{common,control,loader,pg-sink,pg-to-arrow}/src` and `tests/e2e/src` only.
operation = Document every item reachable from the five library roots, including public fields and enum variants; describe invariants, units, ownership, and side effects, not implementation syntax.
proof = `RUSTDOCFLAGS='-D missing_docs -D rustdoc::broken_intra_doc_links' cargo doc --workspace --no-deps` succeeds after excluding binaries only through rustdoc's normal reachability rules.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n '^\s*pub (async )?(unsafe )?(fn|struct|enum|trait|type|mod|const|static)|^\s*pub [A-Za-z_][A-Za-z0-9_]*\s*:' crates tests/e2e/src --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test -p common -p control -p loader -p pg-sink -p pg-to-arrow -p e2e
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] `RUSTDOCFLAGS='-D missing_docs -D rustdoc::broken_intra_doc_links' cargo doc --workspace --no-deps` succeeds after excluding binaries only through rustdoc's normal reachability rules.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=29.2
rule=doc-all-public
outcome=change
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=none
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `doc-all-public`](../../../.claude/skills/rust-skills/rules/doc-all-public.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
