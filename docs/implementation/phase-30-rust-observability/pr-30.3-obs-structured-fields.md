<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 30.3 — Move tracing values into stable structured fields

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
>
> **Gates:** fmt,clippy,test · **Test packages:** common,loader,pg-sink

> **Phase:** 30 — Rust observability · **Crates touched:** `common`, `loader`, `pg-sink` · **Est. size:** M ·
> **Depends on:** PR 30.2 · **Unlocks:** PR 30.4

Apply [`obs-structured-fields`](../../../.claude/skills/rust-skills/rules/obs-structured-fields.md) to Walrus with the
predetermined disposition above. The rule is: **Record structured key-value fields, not values interpolated into the message string**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `obs-structured-fields` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Several loader and sink events interpolate errors or identifiers into message text even though common::telemetry defines stable structured-field keys.

## Read first

- [`obs-structured-fields`](../../../.claude/skills/rust-skills/rules/obs-structured-fields.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Move every dynamic value in a matching tracing message into a named field, use canonical keys from `common::telemetry::fields` where defined, and leave the message as stable prose.
- Prove this postcondition: The probe has zero production hits, telemetry tests assert representative JSON field names, and both packages pass.

**Explicitly deferred** (do not build these here)

- Preserve the pre-subscriber `eprintln!` fallbacks and do not log SQL, credentials, or full configuration.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'tracing::(trace|debug|info|warn|error)!\([^;]*\{[A-Za-z_][A-Za-z0-9_]*(?::[#?])?\}' crates/{loader,pg-sink}/src --glob '*.rs' || true
```

Several loader and sink events interpolate errors or identifiers into message text even though common::telemetry defines stable structured-field keys.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

## Implementation contract

**Allowed files:** Production Rust files under `crates/loader/src` and `crates/pg-sink/src` containing probe hits, plus `crates/common/src/telemetry_test.rs`.

**Required action:** Move every dynamic value in a matching tracing message into a named field, use canonical keys from `common::telemetry::fields` where defined, and leave the message as stable prose.

**Acceptance:** The probe has zero production hits, telemetry tests assert representative JSON field names, and both packages pass.

**Baseline mismatch:** If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

**Evidence note schema:** No evidence note is authorized by this change ticket. A baseline mismatch requires re-auditing and re-authoring the task before it can be selected again.

## Files to create / modify

```text
crates/loader/src
crates/pg-sink/src
crates/common/src/telemetry_test.rs
```

## Skeleton

```text
target = Production Rust files under `crates/loader/src` and `crates/pg-sink/src` containing probe hits, plus `crates/common/src/telemetry_test.rs`.
operation = Move every dynamic value in a matching tracing message into a named field, use canonical keys from `common::telemetry::fields` where defined, and leave the message as stable prose.
proof = The probe has zero production hits, telemetry tests assert representative JSON field names, and both packages pass.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = ! rg -n 'tracing::(trace|debug|info|warn|error)!\([^;]*\{[A-Za-z_][A-Za-z0-9_]*(?::[#?])?\}' crates/{loader,pg-sink}/src --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test -p common -p loader -p pg-sink
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The probe has zero production hits, telemetry tests assert representative JSON field names, and both packages pass.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=30.3
rule=obs-structured-fields
outcome=change
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=none
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `obs-structured-fields`](../../../.claude/skills/rust-skills/rules/obs-structured-fields.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
