<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 30.1 — Verify tracing-only diagnostics after subscriber initialization

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 30 — Rust observability · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 29.12 · **Unlocks:** PR 30.2

Apply [`obs-tracing-over-log`](../../../.claude/skills/rust-skills/rules/obs-tracing-over-log.md) to Walrus with the
predetermined disposition above. The rule is: **Use `tracing` for structured, span-aware diagnostics instead of `println!` or bare `log`**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `obs-tracing-over-log` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Production diagnostics already use tracing; the only `eprintln!` calls are binary bootstrap failures that occur before a subscriber can be installed.

## Read first

- [`obs-tracing-over-log`](../../../.claude/skills/rust-skills/rules/obs-tracing-over-log.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Classify each stderr bootstrap fallback, confirm no library or post-init path prints directly, and record why those pre-subscriber errors must remain visible without tracing.
- Prove this postcondition: The note accounts for every direct-print hit and identifies any post-init hit as a blocking contradiction rather than an optional cleanup.

**Explicitly deferred** (do not build these here)

- Do not route tracing-initialization failure through the subscriber that failed to initialize.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'println!|eprintln!|log::' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' --glob '!**/benches/**' || true
```

Production diagnostics already use tracing; the only `eprintln!` calls are binary bootstrap failures that occur before a subscriber can be installed.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/obs-tracing-over-log.md` only.

**Required action:** Classify each stderr bootstrap fallback, confirm no library or post-init path prints directly, and record why those pre-subscriber errors must remain visible without tracing.

**Acceptance:** The note accounts for every direct-print hit and identifies any post-init hit as a blocking contradiction rather than an optional cleanup.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/obs-tracing-over-log.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/obs-tracing-over-log.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/obs-tracing-over-log.md` only.
operation = Classify each stderr bootstrap fallback, confirm no library or post-init path prints directly, and record why those pre-subscriber errors must remain visible without tracing.
proof = The note accounts for every direct-print hit and identifies any post-init hit as a blocking contradiction rather than an optional cleanup.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'println!|eprintln!|log::' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' --glob '!**/benches/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note accounts for every direct-print hit and identifies any post-init hit as a blocking contradiction rather than an optional cleanup.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=30.1
rule=obs-tracing-over-log
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/obs-tracing-over-log.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `obs-tracing-over-log`](../../../.claude/skills/rust-skills/rules/obs-tracing-over-log.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
