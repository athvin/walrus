<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 30.2 — Verify subscriber initialization stays in binary bootstrap

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 30 — Rust observability · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 30.1 · **Unlocks:** PR 30.3

Apply [`obs-library-facade`](../../../.claude/skills/rust-skills/rules/obs-library-facade.md) to Walrus with the
predetermined disposition above. The rule is: **Libraries emit through the tracing/log facade and never install a subscriber**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `obs-library-facade` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Subscriber construction is centralized in `common::telemetry::init_tracing` and invoked only from the two owning binaries; other library code only emits events.

## Read first

- [`obs-library-facade`](../../../.claude/skills/rust-skills/rules/obs-library-facade.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Trace every initializer call from `main`, verify it is absent from reusable library operations/tests, and document common::telemetry as an internal application bootstrap adapter rather than a self-initializing library.
- Prove this postcondition: The note lists the initializer definition and all call sites and confirms a second installation is handled without panic.

**Explicitly deferred** (do not build these here)

- Do not duplicate subscriber setup in binaries or remove the shared configuration policy.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'init_tracing|tracing_subscriber|\.init\(\)|\.try_init\(' crates --glob '*.rs' || true
```

Subscriber construction is centralized in `common::telemetry::init_tracing` and invoked only from the two owning binaries; other library code only emits events.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/obs-library-facade.md` only.

**Required action:** Trace every initializer call from `main`, verify it is absent from reusable library operations/tests, and document common::telemetry as an internal application bootstrap adapter rather than a self-initializing library.

**Acceptance:** The note lists the initializer definition and all call sites and confirms a second installation is handled without panic.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/obs-library-facade.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/obs-library-facade.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/obs-library-facade.md` only.
operation = Trace every initializer call from `main`, verify it is absent from reusable library operations/tests, and document common::telemetry as an internal application bootstrap adapter rather than a self-initializing library.
proof = The note lists the initializer definition and all call sites and confirms a second installation is handled without panic.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'init_tracing|tracing_subscriber|\.init\(\)|\.try_init\(' crates --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note lists the initializer definition and all call sites and confirms a second installation is handled without panic.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=30.2
rule=obs-library-facade
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/obs-library-facade.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `obs-library-facade`](../../../.claude/skills/rust-skills/rules/obs-library-facade.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
