<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 31.4 — Verify map updates already use the correct entry semantics

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 31 — Rust performance patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 31.3 · **Unlocks:** PR 31.5

Apply [`perf-entry-api`](../../../.claude/skills/rust-skills/rules/perf-entry-api.md) to Walrus with the
predetermined disposition above. The rule is: **Use entry API for map insert-or-update**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `perf-entry-api` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Insert-or-update paths in stream_txn, consume, memory, and ddl already use entry; remaining inserts are unconditional cache replacement or hydration.

## Read first

- [`perf-entry-api`](../../../.claude/skills/rust-skills/rules/perf-entry-api.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Audit each map insertion with its preceding lookup and record whether replacement, duplicate rejection, or entry mutation is intended.
- Prove this postcondition: The note proves every insert-or-update uses entry and names unconditional replacement sites as intentional.

**Explicitly deferred** (do not build these here)

- Do not convert replacement inserts to `or_insert` or change duplicate semantics.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'HashMap|BTreeMap|\.entry\(|\.get\(|\.insert\(' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' || true
```

Insert-or-update paths in stream_txn, consume, memory, and ddl already use entry; remaining inserts are unconditional cache replacement or hydration.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/perf-entry-api.md` only.

**Required action:** Audit each map insertion with its preceding lookup and record whether replacement, duplicate rejection, or entry mutation is intended.

**Acceptance:** The note proves every insert-or-update uses entry and names unconditional replacement sites as intentional.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/perf-entry-api.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/perf-entry-api.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/perf-entry-api.md` only.
operation = Audit each map insertion with its preceding lookup and record whether replacement, duplicate rejection, or entry mutation is intended.
proof = The note proves every insert-or-update uses entry and names unconditional replacement sites as intentional.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'HashMap|BTreeMap|\.entry\(|\.get\(|\.insert\(' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note proves every insert-or-update uses entry and names unconditional replacement sites as intentional.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=31.4
rule=perf-entry-api
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/perf-entry-api.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `perf-entry-api`](../../../.claude/skills/rust-skills/rules/perf-entry-api.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
