<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 31.9 — Verify every Criterion target prevents dead-code elimination

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 31 — Rust performance patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 31.8 · **Unlocks:** PR 31.10

Apply [`perf-black-box-bench`](../../../.claude/skills/rust-skills/rules/perf-black-box-bench.md) to Walrus with the
predetermined disposition above. The rule is: **Use black_box in benchmarks**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `perf-black-box-bench` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: All Criterion targets already black-box benchmark inputs or outputs at the optimization boundary; loader transform uses iter_batched inputs whose outputs Criterion consumes.

## Read first

- [`perf-black-box-bench`](../../../.claude/skills/rust-skills/rules/perf-black-box-bench.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Inventory each benchmark closure, name the value preventing dead-code elimination, and run `cargo bench --no-run` for all benchmark packages.
- Prove this postcondition: The note accounts for every benchmark and flags a closure only if neither Criterion nor explicit black_box consumes its result.

**Explicitly deferred** (do not build these here)

- Do not sprinkle black_box inside the operation being measured or change benchmark workloads.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'black_box|bench_function|iter_batched' crates/*/benches --glob '*.rs' || true
```

All Criterion targets already black-box benchmark inputs or outputs at the optimization boundary; loader transform uses iter_batched inputs whose outputs Criterion consumes.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/perf-black-box-bench.md` only.

**Required action:** Inventory each benchmark closure, name the value preventing dead-code elimination, and run `cargo bench --no-run` for all benchmark packages.

**Acceptance:** The note accounts for every benchmark and flags a closure only if neither Criterion nor explicit black_box consumes its result.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/perf-black-box-bench.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/perf-black-box-bench.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/perf-black-box-bench.md` only.
operation = Inventory each benchmark closure, name the value preventing dead-code elimination, and run `cargo bench --no-run` for all benchmark packages.
proof = The note accounts for every benchmark and flags a closure only if neither Criterion nor explicit black_box consumes its result.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'black_box|bench_function|iter_batched' crates/*/benches --glob '*.rs'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note accounts for every benchmark and flags a closure only if neither Criterion nor explicit black_box consumes its result.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=31.9
rule=perf-black-box-bench
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/perf-black-box-bench.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `perf-black-box-bench`](../../../.claude/skills/rust-skills/rules/perf-black-box-bench.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
