<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 31.6 — Record why stateful builder loops do not use `extend`

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 31 — Rust performance patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 31.5 · **Unlocks:** PR 31.7

Apply [`perf-extend-batch`](../../../.claude/skills/rust-skills/rules/perf-extend-batch.md) to Walrus with the
predetermined disposition above. The rule is: **Use extend for batch insertions**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `perf-extend-batch` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Repeated pushes in production either compute fallible/stateful values per iteration or update Arrow builders that do not expose equivalent Extend semantics.

## Read first

- [`perf-extend-batch`](../../../.claude/skills/rust-skills/rules/perf-extend-batch.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Inspect loops that only push, separate pure mapping from fallible/stateful mutation, and record the consumer type's actual batch API.
- Prove this postcondition: The note lists every credible pure-push candidate and explains why none gains a simpler or faster extend call.

**Explicitly deferred** (do not build these here)

- Do not collect merely to call extend or hide per-item error propagation.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'for .*\{|\.push\(|\.extend\(' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' || true
```

Repeated pushes in production either compute fallible/stateful values per iteration or update Arrow builders that do not expose equivalent Extend semantics.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/perf-extend-batch.md` only.

**Required action:** Inspect loops that only push, separate pure mapping from fallible/stateful mutation, and record the consumer type's actual batch API.

**Acceptance:** The note lists every credible pure-push candidate and explains why none gains a simpler or faster extend call.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/perf-extend-batch.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/perf-extend-batch.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/perf-extend-batch.md` only.
operation = Inspect loops that only push, separate pure mapping from fallible/stateful mutation, and record the consumer type's actual batch API.
proof = The note lists every credible pure-push candidate and explains why none gains a simpler or faster extend call.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'for .*\{|\.push\(|\.extend\(' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note lists every credible pure-push candidate and explains why none gains a simpler or faster extend call.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=31.6
rule=perf-extend-batch
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/perf-extend-batch.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `perf-extend-batch`](../../../.claude/skills/rust-skills/rules/perf-extend-batch.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
