<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 31.12 — Record why the default hasher remains the safe baseline

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 31 — Rust performance patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 31.11 · **Unlocks:** PR 31.13

Apply [`perf-ahash`](../../../.claude/skills/rust-skills/rules/perf-ahash.md) to Walrus with the
predetermined disposition above. The rule is: **Use a faster hasher (`ahash` / `FxHashMap`) when DoS resistance is not needed**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `perf-ahash` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: Maps accept source-derived relation IDs, schema/table names, and transaction IDs; no benchmark attributes material cost to SipHash, and adversarial inputs are not categorically excluded.

## Read first

- [`perf-ahash`](../../../.claude/skills/rust-skills/rules/perf-ahash.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Inventory every map key, trust boundary, maximum cardinality, and benchmark coverage; record why the default hasher remains the safe baseline.
- Prove this postcondition: The note requires a profile showing hashing as a bottleneck plus a documented DoS boundary before reconsidering.

**Explicitly deferred** (do not build these here)

- Do not add ahash/FxHash or globally alias HashMap without evidence.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'HashMap|HashSet|AHash|FxHash' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' || true
```

Maps accept source-derived relation IDs, schema/table names, and transaction IDs; no benchmark attributes material cost to SipHash, and adversarial inputs are not categorically excluded.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/perf-ahash.md` only.

**Required action:** Inventory every map key, trust boundary, maximum cardinality, and benchmark coverage; record why the default hasher remains the safe baseline.

**Acceptance:** The note requires a profile showing hashing as a bottleneck plus a documented DoS boundary before reconsidering.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/perf-ahash.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/perf-ahash.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/perf-ahash.md` only.
operation = Inventory every map key, trust boundary, maximum cardinality, and benchmark coverage; record why the default hasher remains the safe baseline.
proof = The note requires a profile showing hashing as a bottleneck plus a documented DoS boundary before reconsidering.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'HashMap|HashSet|AHash|FxHash' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note requires a profile showing hashing as a bottleneck plus a documented DoS boundary before reconsidering.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=31.12
rule=perf-ahash
outcome=evidence
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/perf-ahash.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `perf-ahash`](../../../.claude/skills/rust-skills/rules/perf-ahash.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
