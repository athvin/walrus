<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 34.8 — Record PR 10.9 as the owner of production panic enforcement

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** superseded by PR 10.9
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 34 — Rust anti-patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 34.7 · **Unlocks:** PR 34.9

Apply [`anti-panic-expected`](../../../.claude/skills/rust-skills/rules/anti-panic-expected.md) to Walrus with the
predetermined disposition above. The rule is: **Don't panic on expected or recoverable errors**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `anti-panic-expected` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: PR 10.9 denies production panic macros, converts recoverable/impossible paths to Result/exhaustive matches, and pins only the deferred stub.

## Read first

- [`anti-panic-expected`](../../../.claude/skills/rust-skills/rules/anti-panic-expected.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Account for every residual macro against PR 10.9's narrow expectation and verify no runtime input reaches it.
- Prove this postcondition: The note reports no panic for I/O, parsing, network, database, or user-controlled state.

**Explicitly deferred** (do not build these here)

- Do not remove the documented deferred-goal seam or weaken the lints.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'panic!|todo!|unimplemented!|unreachable!' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' --glob '!**/benches/**' || true
```

PR 10.9 denies production panic macros, converts recoverable/impossible paths to Result/exhaustive matches, and pins only the deferred stub.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/anti-panic-expected.md` only.

**Required action:** Account for every residual macro against PR 10.9's narrow expectation and verify no runtime input reaches it.

**Acceptance:** The note reports no panic for I/O, parsing, network, database, or user-controlled state.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/anti-panic-expected.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/anti-panic-expected.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/anti-panic-expected.md` only.
operation = Account for every residual macro against PR 10.9's narrow expectation and verify no runtime input reaches it.
proof = The note reports no panic for I/O, parsing, network, database, or user-controlled state.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n 'panic!|todo!|unimplemented!|unreachable!' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' --glob '!**/benches/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note reports no panic for I/O, parsing, network, database, or user-controlled state.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=34.8
rule=anti-panic-expected
outcome=superseded by PR 10.9
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/anti-panic-expected.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `anti-panic-expected`](../../../.claude/skills/rust-skills/rules/anti-panic-expected.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
