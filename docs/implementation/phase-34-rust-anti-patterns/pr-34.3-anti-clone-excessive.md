<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 34.3 — Record PR 9.1 as the owner of the clone audit

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** superseded by PR 9.1
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 34 — Rust anti-patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 34.2 · **Unlocks:** PR 34.4

Apply [`anti-clone-excessive`](../../../.claude/skills/rust-skills/rules/anti-clone-excessive.md) to Walrus with the
predetermined disposition above. The rule is: **Don't clone when borrowing works**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `anti-clone-excessive` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: PR 9.1 owns the borrow-over-clone audit and records remaining ownership-transfer/async/task-boundary clones; this anti-rule adds no new decision boundary.

## Read first

- [`anti-clone-excessive`](../../../.claude/skills/rust-skills/rules/anti-clone-excessive.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Re-run the clone inventory, compare it to PR 9.1's retained exceptions, and record only new residual sites.
- Prove this postcondition: The note reports zero unclassified post-9.1 clones or identifies a regression that must be fixed under the existing policy.

**Explicitly deferred** (do not build these here)

- Do not redesign APIs or lifetimes after the owning audit.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '\.clone\(|Arc::clone|clone_from' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' || true
```

PR 9.1 owns the borrow-over-clone audit and records remaining ownership-transfer/async/task-boundary clones; this anti-rule adds no new decision boundary.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/anti-clone-excessive.md` only.

**Required action:** Re-run the clone inventory, compare it to PR 9.1's retained exceptions, and record only new residual sites.

**Acceptance:** The note reports zero unclassified post-9.1 clones or identifies a regression that must be fixed under the existing policy.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/anti-clone-excessive.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/anti-clone-excessive.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/anti-clone-excessive.md` only.
operation = Re-run the clone inventory, compare it to PR 9.1's retained exceptions, and record only new residual sites.
proof = The note reports zero unclassified post-9.1 clones or identifies a regression that must be fixed under the existing policy.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n '\.clone\(|Arc::clone|clone_from' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note reports zero unclassified post-9.1 clones or identifies a regression that must be fixed under the existing policy.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=34.3
rule=anti-clone-excessive
outcome=superseded by PR 9.1
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/anti-clone-excessive.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `anti-clone-excessive`](../../../.claude/skills/rust-skills/rules/anti-clone-excessive.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
