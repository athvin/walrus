<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 34.2 — Record PR 7.7 as the owner of production `expect` enforcement

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** superseded by PR 7.7
>
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 34 — Rust anti-patterns · **Crates touched:** none (evidence note only) · **Est. size:** S ·
> **Depends on:** PR 34.1 · **Unlocks:** PR 34.3

Apply [`anti-expect-lazy`](../../../.claude/skills/rust-skills/rules/anti-expect-lazy.md) to Walrus with the
predetermined disposition above. The rule is: **Don't use expect for recoverable errors**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `anti-expect-lazy` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: PRs 7.6-7.7 deny production expect; the single metrics recorder invariant has a narrow documented allow because failure means conflicting process-global initialization.

## Read first

- [`anti-expect-lazy`](../../../.claude/skills/rust-skills/rules/anti-expect-lazy.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Account for every production hit, verify the metrics allow reason and OnceLock guard, and record all test/bench exemptions separately.
- Prove this postcondition: The note proves no expect handles a recoverable I/O, input, network, or database error.

**Explicitly deferred** (do not build these here)

- Do not replace the intentional invariant without an API-level recorder initialization design.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n '\.expect\(' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' --glob '!**/benches/**' || true
```

PRs 7.6-7.7 deny production expect; the single metrics recorder invariant has a narrow documented allow because failure means conflicting process-global initialization.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

## Implementation contract

**Allowed files:** `docs/implementation/notes/rust-skills/anti-expect-lazy.md` only.

**Required action:** Account for every production hit, verify the metrics allow reason and OnceLock guard, and record all test/bench exemptions separately.

**Acceptance:** The note proves no expect handles a recoverable I/O, input, network, or database error.

**Baseline mismatch:** The evidence disposition and rule-named note are fixed. A contradictory baseline blocks implementation and requires the ticket to be re-audited and re-authored before selection.

**Evidence note schema:** `docs/implementation/notes/rust-skills/anti-expect-lazy.md` records the audited base commit, every exact command and result, findings classified by stable path/symbol, the predetermined conclusion, and the concrete reversal condition.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/anti-expect-lazy.md
```

## Skeleton

```text
target = `docs/implementation/notes/rust-skills/anti-expect-lazy.md` only.
operation = Account for every production hit, verify the metrics allow reason and OnceLock guard, and record all test/bench exemptions separately.
proof = The note proves no expect handles a recoverable I/O, input, network, or database error.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = rg -n '\.expect\(' crates --glob '*.rs' --glob '!**/*_test.rs' --glob '!**/tests/**' --glob '!**/benches/**'
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test --workspace
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The note proves no expect handles a recoverable I/O, input, network, or database error.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=34.2
rule=anti-expect-lazy
outcome=superseded by PR 7.7
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=docs/implementation/notes/rust-skills/anti-expect-lazy.md
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `anti-expect-lazy`](../../../.claude/skills/rust-skills/rules/anti-expect-lazy.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
