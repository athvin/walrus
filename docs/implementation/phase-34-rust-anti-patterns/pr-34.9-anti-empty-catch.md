<!-- Canonical one-rule Rust curriculum ticket. Generated tickets start draft; audit before activation. -->

# PR 34.9 — Log discarded cleanup and task-join errors

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
>
> **Gates:** fmt,clippy,test · **Test packages:** loader,pg-sink

> **Phase:** 34 — Rust anti-patterns · **Crates touched:** `loader`, `pg-sink` · **Est. size:** M ·
> **Depends on:** PR 34.8 · **Unlocks:** PR 34.10

Apply [`anti-empty-catch`](../../../.claude/skills/rust-skills/rules/anti-empty-catch.md) to Walrus with the
predetermined disposition above. The rule is: **Don't silently ignore errors**. This is not an open-ended
cleanup ticket: the probe, allowed files, action, and baseline-mismatch handling below are the complete contract.

## Why — learning objectives

- Practise the `anti-empty-catch` rule at the exact Walrus boundary identified by the baseline, including the
  exceptions in the source rule rather than applying its example mechanically.
- Preserve the project-specific trade-off in this audit: A small set of best-effort rollback, object deletion, and task-join paths discard errors without an event; parse-to-Option conversions are intentional and must remain.

## Read first

- [`anti-empty-catch`](../../../.claude/skills/rust-skills/rules/anti-empty-catch.md), including its exceptions.
- [`task-conventions.md`](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md).
- The complete modules/configuration named under Allowed files; grep matches are evidence, not edit instructions.

## Scope

**In scope**

- Capture the baseline below after every predecessor has merged.
- Modify only the canonical file list and perform this exact operation: Replace discarded cleanup/join Results with explicit `if let Err(e)` warning events carrying operation and safe identifiers; retain parser `.ok()` conversions whose return type intentionally discards parse detail.
- Prove this postcondition: The probe leaves only classified parse-to-Option/test sites, every retained discard has an adjacent rationale, and both service packages pass.

**Explicitly deferred** (do not build these here)

- Do not turn best-effort cleanup failure into a primary operation failure or log credentials/object payloads.
- Preserve CDC ordering `(commit_lsn, lsn)`, SQL/query bytes, public behavior, and existing error
  classification unless the required action explicitly names them.

## Baseline and decision

```bash
rg -n 'let _ =|if let Err\(_\)|Err\(_\) => \{\}|\.ok\(\)' crates/{loader,pg-sink}/src --glob '*.rs' --glob '!**/*_test.rs' || true
```

A small set of best-effort rollback, object deletion, and task-join paths discard errors without an event; parse-to-Option conversions are intentional and must remain.

**Baseline precondition:** the command must reproduce that finding after all predecessors merge.
If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

## Implementation contract

**Allowed files:** `crates/loader/src/compaction.rs`, `crates/loader/src/phase_b.rs`, `crates/loader/src/main.rs`, `crates/pg-sink/src/stream_txn.rs`, and `crates/pg-sink/src/main.rs`.

**Required action:** Replace discarded cleanup/join Results with explicit `if let Err(e)` warning events carrying operation and safe identifiers; retain parser `.ok()` conversions whose return type intentionally discards parse detail.

**Acceptance:** The probe leaves only classified parse-to-Option/test sites, every retained discard has an adjacent rationale, and both service packages pass.

**Baseline mismatch:** If a predecessor already satisfies the acceptance postcondition, or the probe no longer reproduces the audited finding, stop. Re-audit and re-author this ticket before selection; an implementer may not substitute another refactor, a no-op completion, or an evidence-only PR.

**Evidence note schema:** No evidence note is authorized by this change ticket. A baseline mismatch requires re-auditing and re-authoring the task before it can be selected again.

## Files to create / modify

```text
crates/loader/src/compaction.rs
crates/loader/src/phase_b.rs
crates/loader/src/main.rs
crates/pg-sink/src/stream_txn.rs
crates/pg-sink/src/main.rs
```

## Skeleton

```text
target = `crates/loader/src/compaction.rs`, `crates/loader/src/phase_b.rs`, `crates/loader/src/main.rs`, `crates/pg-sink/src/stream_txn.rs`, and `crates/pg-sink/src/main.rs`.
operation = Replace discarded cleanup/join Results with explicit `if let Err(e)` warning events carrying operation and safe identifiers; retain parser `.ok()` conversions whose return type intentionally discards parse detail.
proof = The probe leaves only classified parse-to-Option/test sites, every retained discard has an adjacent rationale, and both service packages pass.
baseline-mismatch = block-for-reauthoring
```

## Verification commands

```text
rule-probe = ! rg -n 'let _ =' crates/loader/src/compaction.rs crates/loader/src/phase_b.rs crates/loader/src/main.rs crates/pg-sink/src/stream_txn.rs crates/pg-sink/src/main.rs
fmt = cargo fmt --check
clippy = cargo clippy --workspace --all-targets --all-features -- -D warnings
test = cargo test -p loader -p pg-sink
docs = cargo doc --workspace --no-deps
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The baseline command and its complete output are recorded in the PR or evidence note.
- [ ] The diff contains only the allowed files and implements the required action.
- [ ] The probe leaves only classified parse-to-Option/test sites, every retained discard has an adjacent rationale, and both service packages pass.
- [ ] No deferred cleanup, dependency, abstraction, documentation, panic, or public API was invented.
- [ ] Every command in **Verification commands** passes and is reported by label.
- [ ] Status remains Planned and the roadmap checkbox remains unchecked until the separate mark-done PR.

## What completed looks like

```text
task=34.9
rule=anti-empty-catch
outcome=change
rule-probe=pass
baseline=recorded-with-output
allowed-files=verified
acceptance=pass
fmt=pass
clippy=pass
test=pass
docs=pass
diff-check=pass
note=none
```

## Hints & gotchas

- A search hit is a candidate, not permission to edit it; read the complete containing module.
- If the baseline contradicts the audited finding or already satisfies a change task, stop and
  re-author the ticket before it is selected again.
- Evidence and superseded outcomes create the named note only; a low-signal tripwire or dependency
  is not required to make the task substantial.

## References

- [Rust rule: `anti-empty-catch`](../../../.claude/skills/rust-skills/rules/anti-empty-catch.md)
- [Implementation curriculum](../README.md)
- [Task conventions](../../../.claude/skills/implementing-walrus-roadmap/reference/task-conventions.md)
