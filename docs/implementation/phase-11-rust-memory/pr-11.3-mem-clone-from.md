<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 11.3 — Reuse the remaining batch-id assignment with clone_from

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
> **Gates:** fmt,clippy,test · **Test packages:** pg-sink

> **Phase:** 11 — Rust memory optimization · **Crates touched:** `pg-sink` ·
> **Est. size:** S · **Depends on:** PR 11.2 · **Unlocks:** PR 11.4

PR 9.3 already changes the loader rename fold and enables `clippy::assigning_clones`. The sole residual site is `TableBatcher::on_commit`, where the destination `String` can reuse its buffer.

## Why — learning objectives

- Apply `clone_from` only to assignment into an existing allocation.
- Treat the earlier lint and loader edit as owned state, not work to repeat.

## Read first

- [`mem-clone-from`](../../../.claude/skills/rust-skills/rules/mem-clone-from.md) — the source rule.
- `crates/pg-sink/src/batch.rs` — `TableBatcher::on_commit` and its `meta.batch_id` assignment.
- `crates/pg-sink/src/batch_test.rs` — batch promotion coverage.

## Baseline contract

- **Precondition:** Confirm the rule and every named symbol on the immediate predecessor commit with `rg`; historical line numbers are not part of the contract.
- **Allowed files:** The **Files to create / modify** block is exhaustive.


## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Replace only `meta.batch_id = self.batch_id.clone()` with `meta.batch_id.clone_from(&self.batch_id)`.
- Add a focused sibling test proving committed rows retain the byte-identical batch id.
- Leave PR 9.3's loader edit and workspace lint untouched.

**Explicitly deferred**

- No other clone sweep and no `Arc<str>` conversion.
- No `Cargo.toml` or loader change.

## Files to create / modify

```text
crates/pg-sink/src/batch.rs
crates/pg-sink/src/batch_test.rs
```

## Skeleton

```rust
meta.batch_id.clone_from(&self.batch_id);
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/mem-clone-from.md
focused-test = cargo test -p pg-sink
clone-from-site = rg -n 'batch_id\.clone_from' crates/pg-sink/src/batch.rs
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The one residual assignment uses `clone_from`.
- [ ] The sibling test proves output identity.
- [ ] No loader, manifest, or workspace-lint file changes.

## What completed looks like

`TableBatcher::on_commit` reuses the destination allocation and the pg-sink suite stays green.

## Hints & gotchas

The destination is empty in today's construction path; this task teaches and pins assignment shape without claiming an unmeasured throughput win.

## References

- Rule: [`mem-clone-from`](../../../.claude/skills/rust-skills/rules/mem-clone-from.md)
- [Roadmap](../README.md) · Prev: PR 11.2 · Next: PR 11.4
