<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 11.8 — Tighten the residual loader RefCell borrow lifetime

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
> **Gates:** fmt,clippy,test · **Test packages:** loader

> **Phase:** 11 — Rust memory optimization · **Crates touched:** `loader` ·
> **Est. size:** S · **Depends on:** PR 11.7 · **Unlocks:** PR 11.9

PR 9.11 owns the reload waiter lock scope and both significant-drop lints. This task handles only the remaining loader cache borrow that can otherwise overlap a mutable borrow.

## Why — learning objectives

- Make a `RefCell` guard's drop point explicit before branching.
- Preserve earlier lock-policy ownership instead of repeating it.

## Read first

- [`mem-drop-order`](../../../.claude/skills/rust-skills/rules/mem-drop-order.md) — the source rule.
- `crates/loader/src/duck.rs` — `TableDb::columns_for`.
- `crates/loader/src/duck_test.rs` — cache hit and miss behavior.

## Baseline contract

- **Precondition:** Confirm the rule and every named symbol on the immediate predecessor commit with `rg`; historical line numbers are not part of the contract.
- **Allowed files:** The **Files to create / modify** block is exhaustive.


## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- In `TableDb::columns_for`, borrow the cache, clone the cached `Arc` into a local option, explicitly end the borrow, then branch.
- Add a sibling regression test that exercises a cache hit followed by the mutation-capable miss path.
- Do not edit the reload registry or lint configuration already owned by PR 9.11.

**Explicitly deferred**

- No lock type changes, field reordering, `ManuallyDrop`, or cache redesign.

## Files to create / modify

```text
crates/loader/src/duck.rs
crates/loader/src/duck_test.rs
```

## Skeleton

```rust
let cached = { self.parquet_cols.borrow().get(table).cloned() };
if let Some(columns) = cached { return Ok(columns); }
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/mem-drop-order.md
focused-test = cargo test -p loader
borrow-scope = rg -n 'let cached|parquet_cols\.borrow' crates/loader/src/duck.rs
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The cache borrow ends before the hit/miss branch.
- [ ] The regression test covers both branches in sequence.
- [ ] No `Cargo.toml` or pg-sink file changes.

## What completed looks like

The cache returns identical values while no `RefCell` borrow spans the branch body.

## Hints & gotchas

Use a block expression to delimit the borrow; do not add a manual `drop` when lexical scope is sufficient.

## References

- Rule: [`mem-drop-order`](../../../.claude/skills/rust-skills/rules/mem-drop-order.md)
- [Roadmap](../README.md) · Prev: PR 11.7 · Next: PR 11.9
