<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 11.13 — Measure and decline SmallVec for key-column scratch

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
> **Gates:** fmt,clippy,test · **Test packages:** loader

> **Phase:** 11 — Rust memory optimization · **Crates touched:** `loader` bench only ·
> **Est. size:** S · **Depends on:** PR 11.12 · **Unlocks:** PR 11.14

The audited system profile shows DuckDB work dominates the transform cycle, so this task records a focused measurement but does not add or promote `smallvec` regardless of the micro-benchmark result.

## Why — learning objectives

- A null or local micro-benchmark result is evidence, not permission to expand the dependency surface.
- System-level bottleneck evidence takes precedence over a tiny allocation in a non-dominant path.

## Read first

- [`mem-smallvec`](../../../.claude/skills/rust-skills/rules/mem-smallvec.md) — the source rule.
- `crates/common/src/pg_shape.rs` — `PgRelation::key_columns`.
- `crates/loader/benches/transform.rs` and `docs/benchmarks.md` — the committed loader baseline.

## Baseline contract

- **Precondition:** Confirm the audited non-applicability or rejected tradeoff on the immediate predecessor commit by symbol.
- **Locked outcome:** Land only the named evidence and guard artifacts; do not adopt the rejected code or dependency shape.
- **Allowed files:** The **Files to create / modify** block is exhaustive. Any applicable production change or ambiguous predecessor drift blocks the task.

## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Add an isolated `loader/keycols` criterion group for one-key and three-key relations.
- Record medians beside the committed transform-cycle numbers.
- Create `docs/implementation/notes/rust-skills/mem-smallvec.md` with verdict **declined**, evidence, and a reversal condition.
- Keep `Cargo.toml`, crate manifests, and `Cargo.lock` unchanged.

**Explicitly deferred**

- No `smallvec`, `arrayvec`, or iterator API rewrite.
- No decision branch remains for the implementer.

## Files to create / modify

```text
crates/loader/benches/transform.rs
docs/benchmarks.md
docs/implementation/notes/rust-skills/mem-smallvec.md
```

## Skeleton

```text
Status: declined
Evidence: loader/keycols median compared with loader/transform
Revisit only when profiling makes key-column collection a material end-to-end cost.
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/mem-smallvec.md
focused-test = cargo test -p loader
keycols-bench = cargo bench -p loader --bench transform -- loader/keycols
evidence-note = test -s docs/implementation/notes/rust-skills/mem-smallvec.md
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The benchmark and benchmark-history entry report the measured number.
- [ ] The canonical note records a declined verdict and reversal condition.
- [ ] No dependency or production source changed.

## What completed looks like

The measurement is reproducible and the dependency count remains unchanged.

## Hints & gotchas

Do not claim a speedup from noise outside criterion's confidence interval; the outcome is already locked to evidence.

## References

- Rule: [`mem-smallvec`](../../../.claude/skills/rust-skills/rules/mem-smallvec.md)
- [Roadmap](../README.md) · Prev: PR 11.12 · Next: PR 11.14
