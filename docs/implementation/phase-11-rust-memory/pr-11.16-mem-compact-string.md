<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 11.16 — Re-measure and re-affirm the SinkMeta string-layout defer

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** evidence
> **Gates:** fmt,clippy,test · **Test packages:** pg-to-arrow

> **Phase:** 11 — Rust memory optimization · **Crates touched:** `pg-to-arrow` bench only ·
> **Est. size:** S · **Depends on:** PR 11.15 · **Unlocks:** PR 11.17

PR 11.3 removes the relevant repeated assignment allocation, while the committed end-to-end profile says the sink is not the bottleneck. This task re-measures and records the defer; it does not adopt `Arc<str>` or a compact-string crate.

## Why — learning objectives

- Re-open an optimization only when new system-level evidence overturns the original ranking.
- Price serde feature and wire-contract costs even for a standard-library sharing type.

## Read first

- [`mem-compact-string`](../../../.claude/skills/rust-skills/rules/mem-compact-string.md) — the source rule.
- `docs/benchmarks.md` — PR 5.6 and PR 5.7 bottleneck evidence.
- `crates/common/src/sink_meta.rs` — the JSON wire contract.
- `crates/pg-to-arrow/benches/batch.rs` — the existing append-row benchmark.

## Baseline contract

- **Precondition:** Confirm the audited non-applicability or rejected tradeoff on the immediate predecessor commit by symbol.
- **Locked outcome:** Land only the named evidence and guard artifacts; do not adopt the rejected code or dependency shape.
- **Allowed files:** The **Files to create / modify** block is exhaustive. Any applicable production change or ambiguous predecessor drift blocks the task.

## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Run the existing batch benchmark on the predecessor tree and record the medians in benchmark history.
- Create `docs/implementation/notes/rust-skills/mem-compact-string.md` with verdict **deferred**, the new numbers, serde cost, and reversal condition.
- Make no manifest, lockfile, production source, or serialized-shape change.

**Explicitly deferred**

- No `Arc<str>`, serde `rc` feature, `compact_str`, `smartstring`, or `ecow`.
- No branch based on the micro-benchmark result.

## Files to create / modify

```text
docs/benchmarks.md
docs/implementation/notes/rust-skills/mem-compact-string.md
```

## Skeleton

```text
Status: deferred
Evidence: current append-row medians plus the system bottleneck ranking
Revisit only if sink allocation becomes an end-to-end limiter.
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/mem-compact-string.md
focused-test = cargo test -p pg-to-arrow
batch-bench = cargo bench -p pg-to-arrow --bench batch
evidence-note = test -s docs/implementation/notes/rust-skills/mem-compact-string.md
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The canonical note and benchmark history contain the fresh medians.
- [ ] The note retains the deferred verdict and names a falsifiable reversal condition.
- [ ] No Rust, manifest, lockfile, or wire-format file changed.

## What completed looks like

The moved baseline is documented and compact-string dependency count remains zero.

## Hints & gotchas

A local micro-benchmark delta does not override the end-to-end ranking established by the committed workload.

## References

- Rule: [`mem-compact-string`](../../../.claude/skills/rust-skills/rules/mem-compact-string.md)
- [Roadmap](../README.md) · Prev: PR 11.15 · Next: PR 11.17
