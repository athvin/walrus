<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 18.10 — Add the three residual generic-bound hygiene lints

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 18 — Rust type safety · **Crates touched:** workspace manifest only ·
> **Est. size:** S · **Depends on:** PR 18.9 · **Unlocks:** PR 18.11

PR 9.6 already enables `elidable_lifetime_names` and fixes both sqlx `Encode` impls. This residual task adds only the three non-overlapping bound-hygiene lints.

## Why — learning objectives

- Keep lint ownership single and chronological.
- Prevent duplicated or split bounds without churning already-idiomatic one-bound signatures.

## Read first

- [`type-generic-bounds`](../../../.claude/skills/rust-skills/rules/type-generic-bounds.md) — the source rule.
- `Cargo.toml` — preserve PR 9.6's `elidable_lifetime_names` entry.
- Generic impls across `crates/*/src` — audited clean for the three residual lints.

## Baseline contract

- **Precondition:** Confirm the rule and every named symbol on the immediate predecessor commit with `rg`; historical line numbers are not part of the contract.
- **Allowed files:** The **Files to create / modify** block is exhaustive.


## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Add exactly `type_repetition_in_bounds`, `trait_duplication_in_bounds`, and `multiple_bound_locations` as workspace clippy denies with one rationale comment.
- Make no Rust source edit and preserve PR 9.6's entry verbatim.

**Explicitly deferred**

- No `elidable_lifetime_names` edit, where-clause churn, new bound, or broad pedantic/nursery group.

## Files to create / modify

```text
Cargo.toml
```

## Skeleton

```toml
type_repetition_in_bounds = "deny"
trait_duplication_in_bounds = "deny"
multiple_bound_locations = "deny"
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/type-generic-bounds.md
workspace-test = cargo test --workspace
bound-policy = rg -n '^(type_repetition_in_bounds|trait_duplication_in_bounds|multiple_bound_locations) = \"deny\"$' Cargo.toml
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The three residual lints are denied exactly once.
- [ ] PR 9.6's lifetime lint and source changes are untouched.
- [ ] No Rust source changed.

## What completed looks like

The workspace enforces the three residual generic-bound invariants with no duplicate lifetime work.

## Hints & gotchas

A lint hit after predecessors is a baseline mismatch; block rather than expanding the file allowlist.

## References

- Rule: [`type-generic-bounds`](../../../.claude/skills/rust-skills/rules/type-generic-bounds.md)
- [Roadmap](../README.md) · Prev: PR 18.9 · Next: PR 18.11
