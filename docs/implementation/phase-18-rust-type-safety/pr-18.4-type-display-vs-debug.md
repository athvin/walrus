<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 18.4 — Record that the Debug sweep is owned by PR 13.1

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** superseded by PR 13.1
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 18 — Rust type safety · **Crates touched:** none ·
> **Est. size:** S · **Depends on:** PR 18.3 · **Unlocks:** PR 18.5

PR 13.1 already derives or implements `Debug` for every public type and enables `missing_debug_implementations`. Repeating that sweep would create incompatible hand-written implementations and duplicate lint ownership.

## Why — learning objectives

- Keep the earliest task as the single owner of shared trait policy.
- Verify the distinct `Display` contract without adding another Debug implementation.

## Read first

- [`type-display-vs-debug`](../../../.claude/skills/rust-skills/rules/type-display-vs-debug.md) — the source rule.
- `Cargo.toml` — PR 13.1's `missing_debug_implementations` deny.
- `crates/common/src/lsn.rs` and `ids.rs` — deliberate `Display`/`Debug` forms.

## Baseline contract

- **Precondition:** Confirm PR 13.1 owns the complete code postcondition on the immediate predecessor commit.
- **Locked outcome:** Make no production or test-code change. Record the ownership proof in the named evidence note.
- **Allowed files:** The evidence note is the only artifact. Any residual uncovered site blocks the task for re-audit.

## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Create `docs/implementation/notes/rust-skills/type-display-vs-debug.md`.
- Record PR 13.1 as owner, verify the merged lint and public-type coverage, and document that `Display` is never derived or delegated to `Debug`.
- Name the reversal condition: a public type legitimately exempted from the lint without a reason.

**Explicitly deferred**

- No Rust, test, manifest, lint, derive, or formatter change.

## Files to create / modify

```text
docs/implementation/notes/rust-skills/type-display-vs-debug.md
```

## Skeleton

```text
Status: superseded by PR 13.1
Owned postcondition: public Debug coverage plus missing_debug_implementations = deny
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/type-display-vs-debug.md
workspace-test = cargo test --workspace
debug-policy = rg -n '^missing_debug_implementations = \"deny\"$' Cargo.toml
evidence-note = test -s docs/implementation/notes/rust-skills/type-display-vs-debug.md
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The canonical note proves PR 13.1 owns the full Debug postcondition.
- [ ] The note records the separate Display invariant and reversal condition.
- [ ] No file outside the note changed.

## What completed looks like

The duplicate task closes with evidence and the earlier trait implementations remain untouched.

## Hints & gotchas

If the workspace lint is absent after PR 13.1, block as predecessor drift; do not recreate the sweep here.

## References

- Rule: [`type-display-vs-debug`](../../../.claude/skills/rust-skills/rules/type-display-vs-debug.md)
- [Roadmap](../README.md) · Prev: PR 18.3 · Next: PR 18.5
