<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 14.3 — Complete the residual no-lock-across-await lint policy

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
> **Gates:** fmt,clippy,test · **Test packages:** —

> **Phase:** 14 — Rust async/await · **Crates touched:** workspace manifest only ·
> **Est. size:** S · **Depends on:** PR 14.2 · **Unlocks:** PR 14.4

PR 9.11 already fixes the reload registry and enables both significant-drop lints. This residual task adds only the three await-specific lints that protect async code.

## Why — learning objectives

- Separate guard-drop policy already owned by PR 9.11 from await-specific diagnostics.
- Keep the loader's documented `!Send` DuckDB model distinct from holding a lock across await.

## Read first

- [`async-no-lock-await`](../../../.claude/skills/rust-skills/rules/async-no-lock-await.md) — the source rule.
- `Cargo.toml` — preserve the two PR 9.11 significant-drop entries.
- `crates/loader/src/main.rs` and `duck.rs` — the LocalSet exception, which is not a lock-held-across-await exception.

## Baseline contract

- **Precondition:** Confirm the rule and every named symbol on the immediate predecessor commit with `rg`; historical line numbers are not part of the contract.
- **Allowed files:** The **Files to create / modify** block is exhaustive.


## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Add exactly `await_holding_lock`, `await_holding_refcell_ref`, and `await_holding_invalid_type` as workspace clippy denies with one rationale comment.
- Leave `await-holding-invalid-types` empty and make no source edit.
- Preserve the PR 9.11 significant-drop entries verbatim.

**Explicitly deferred**

- No reload registry edit, lock replacement, invalid-type list, or new allow attribute.

## Files to create / modify

```text
Cargo.toml
```

## Skeleton

```toml
await_holding_lock = "deny"
await_holding_refcell_ref = "deny"
await_holding_invalid_type = "deny"
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/async-no-lock-await.md
workspace-test = cargo test --workspace
await-policy = rg -n '^(await_holding_lock|await_holding_refcell_ref|await_holding_invalid_type) = \"deny\"$' Cargo.toml
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] All three await-specific lints are denied once.
- [ ] PR 9.11's two significant-drop entries are unchanged.
- [ ] No Rust source or `clippy.toml` change.

## What completed looks like

The workspace has three await-specific policy entries and clippy remains green.

## Hints & gotchas

An empty invalid-type list makes that lint a future policy hook; do not populate it with `duckdb::Connection`.

## References

- Rule: [`async-no-lock-await`](../../../.claude/skills/rust-skills/rules/async-no-lock-await.md)
- [Roadmap](../README.md) · Prev: PR 14.2 · Next: PR 14.4
