<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 19.3 — Finish the residual UtcTimestamp conversion API

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
> **Gates:** fmt,clippy,test · **Test packages:** common

> **Phase:** 19 — Rust traits · **Crates touched:** `common` ·
> **Est. size:** S · **Depends on:** PR 19.2 · **Unlocks:** PR 19.4

PR 18.2 already owns every `repr(transparent)` attribute and layout assertion. This residual task adds only the coherent conversion and display API for `UtcTimestamp`.

## Why — learning objectives

- Use a local newtype to provide conversions for a foreign timestamp type.
- Delegate formatting so Display and serde cannot diverge.

## Read first

- [`trait-coherence-newtype`](../../../.claude/skills/rust-skills/rules/trait-coherence-newtype.md) — the source rule.
- `crates/common/src/sink_meta.rs` — merged `UtcTimestamp` representation and serde implementation.
- `crates/common/src/sink_meta_test.rs` — timestamp contract tests.

## Baseline contract

- **Precondition:** Confirm the rule and every named symbol on the immediate predecessor commit with `rg`; historical line numbers are not part of the contract.
- **Allowed files:** The **Files to create / modify** block is exhaustive.


## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Implement `Display` for `UtcTimestamp` by delegating to `jiff::Timestamp`.
- Implement both `From` directions plus `inner(&self)` and `into_inner(self)`.
- Add sibling tests for round-trip conversion and exact Display-versus-Serialize equality.
- Preserve all PR 18.2 repr attributes and layout assertions verbatim.

**Explicitly deferred**

- No repr/layout edit, Lsn/ManifestId edit, duration newtype, SQL, or `.sqlx` change.

## Files to create / modify

```text
crates/common/src/sink_meta.rs
crates/common/src/sink_meta_test.rs
```

## Skeleton

```rust
impl std::fmt::Display for UtcTimestamp { /* delegate to self.0 */ }
impl From<jiff::Timestamp> for UtcTimestamp { /* wrap */ }
impl From<UtcTimestamp> for jiff::Timestamp { /* unwrap */ }
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/trait-coherence-newtype.md
focused-test = cargo test -p common
timestamp-api = rg -n 'impl (std::fmt::Display|From<jiff::Timestamp>) for UtcTimestamp' crates/common/src/sink_meta.rs
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] The two conversions, two accessors, and delegated Display exist.
- [ ] Tests prove conversion round-trip and Display equals serialized text.
- [ ] PR 18.2 layout code is unchanged.

## What completed looks like

`UtcTimestamp` has a coherent local conversion API without duplicating earlier layout work.

## Hints & gotchas

Delegate formatting directly; a separately written RFC-3339 formatter is out of scope and risks wire drift.

## References

- Rule: [`trait-coherence-newtype`](../../../.claude/skills/rust-skills/rules/trait-coherence-newtype.md)
- [Roadmap](../README.md) · Prev: PR 19.2 · Next: PR 19.4
