<!-- Audited Rust-skills roadmap task. Keep status and DoD unchecked until merge. -->

# PR 16.8 — Measure cache footprints and guard only the residual Emit type

> **Status:** 📋 Planned <!-- flip to "✅ Done — <PR url>" when it merges -->

> **Readiness:** audited · **Outcome:** change
> **Gates:** fmt,clippy,test · **Test packages:** pg-to-arrow

> **Phase:** 16 — Rust codegen optimization · **Crates touched:** `pg-to-arrow` plus benchmark docs ·
> **Est. size:** S · **Depends on:** PR 16.7 · **Unlocks:** PR 16.9

PRs 9.7 and 11.2 already own the hot enum size guards. This residual task preserves those ratchets, adds the previously uncovered `Emit` guard, and documents the post-predecessor cache-footprint arithmetic.

## Why — learning objectives

- Measure the merged type layouts rather than weakening earlier exact guards.
- Translate bytes into per-row and cache-line cost without speculative layout churn.

## Read first

- [`opt-cache-friendly`](../../../.claude/skills/rust-skills/rules/opt-cache-friendly.md) — the source rule.
- `crates/pg-to-arrow/src/batch.rs` — private `Emit`.
- `crates/common/src/pg_shape.rs` and `crates/pg-sink/src/pgoutput/mod.rs` — earlier size owners, read-only here.
- `docs/benchmarks.md` — measurement format.

## Baseline contract

- **Precondition:** Confirm the rule and every named symbol on the immediate predecessor commit with `rg`; historical line numbers are not part of the contract.
- **Allowed files:** The **Files to create / modify** block is exhaustive.


## Scope

**Baseline precondition.** Before editing, reproduce the task's authored finding from its named
source paths, symbols, counts, and read-only probes; run the full **Verification commands** block
after implementation. The named sites and allowed paths are the complete task boundary.

**Baseline mismatch.** If the current tree differs from that authored finding, **STOP and request
task re-authoring before editing.** Do not choose another site, implementation, evidence conclusion,
or outcome.

**In scope**

- Measure `Emit`, `TupleValue`, and `Message` after all predecessors.
- Add only a 64-bit exact const guard and explanatory doc line for `Emit`.
- Add a benchmark-doc table with measured sizes, multiplicity, and 64-byte cache-line arithmetic.
- Preserve all earlier guards byte-for-byte.

**Explicitly deferred**

- No `TupleValue` or `Message` source edit, no repr/alignment/field-order change, no boxing, and no dependency.

## Files to create / modify

```text
crates/pg-to-arrow/src/batch.rs
docs/benchmarks.md
```

## Skeleton

```rust
#[cfg(target_pointer_width = "64")]
const _: () = assert!(std::mem::size_of::<Emit>() == N, "Emit is stored once per source column");
```

## Verification commands

```text
rule-present = test -f .claude/skills/rust-skills/rules/opt-cache-friendly.md
focused-test = cargo test -p pg-to-arrow
emit-guard = rg -n 'size_of::<Emit>' crates/pg-to-arrow/src/batch.rs
diff-check = git diff --check origin/main...HEAD
```

## Definition of Done

- [ ] `Emit` has one measured exact guard and explanatory doc.
- [ ] Benchmark docs contain current post-predecessor numbers and cache-line arithmetic.
- [ ] Earlier size guards are unchanged.

## What completed looks like

The residual plan type is guarded and the cache-footprint table reflects the merged tree.

## Hints & gotchas

If an earlier exact guard already covers `Emit`, block as an outcome mismatch instead of adding a duplicate.

## References

- Rule: [`opt-cache-friendly`](../../../.claude/skills/rust-skills/rules/opt-cache-friendly.md)
- [Roadmap](../README.md) · Prev: PR 16.7 · Next: PR 16.9
